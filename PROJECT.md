# quant_factors — 项目目标、现状与路线

> **文档分工（三份）**：本文件讲「是什么 / 走到哪 / 学到什么」；`.trae/rules/rules.md` 讲「怎么思考 / 必须怎样」（IDE 自动注入）；[HANDOFF.md](HANDOFF.md) 讲「现在打什么仗」（**新会话冷启动先读它**）。

## 项目定位

- **名称**：quant_factors（因子 Loop Engineering 系统）
- **总目标（2026-09-07 定稿，双产品共底座）**：
  - **产品一 · 因子机器**：本地大模型驱动、7×24 全自动闭环——取数 → 特征底座 → 自动挖掘 → 独立裁判校验 → 负反馈迭代 → **持续挖掘/验证/维护私有因子库**。
  - **产品二 · 荐股机器（中远期）**：基于因子库每日输出**荐股与操作建议**（`signals/` 生产层，依赖每日 update）。
  - 两者共用同一底座：数据 → 因子库 → 每日信号；update 机制既是数据保鲜，也是"新鲜考卷"的生产线（见验证协议）。
- **可执行域硬约束（2026-09-07 用户定案）**：**只能买卖股票 + 持有现金，无融券**（转融券 2024-07 起暂停至今、券源≈0 系政策现状而非波动）。多空对冲（结论12）降级为"信号价值证据"，不再是产品形态；仓位调节（降贝塔=持现金）是唯一合法对冲通道。
- **当前阶段（第一阶段）目标**：**把数据搞到本地、搞准搞对，形成丰富特征**。特征底座不牢，后面的自动挖掘全是空中楼阁。
- **信念**：全程本地化私有化，数据、模型、因子资产无外泄（硬性约束见 rules.md R4-8）。

## 环境（单一 Python 环境）

**Conda `jaycode`** 承担全部计算与取数：`conda run -n jaycode python -m research.xxx ...`

- **聚宽通道**：jqcli 源码 vendored 于 `.tools/jqcli`（gitignore），以 `pip install -e .tools/jqcli` 装入 `jaycode` 环境（依赖 requests/websocket-client/python-dotenv，与 qlib 无冲突）。凭据只存 `.env` 的 `JQCLI_COOKIE`。
  两步重建：`git clone --depth 1 https://github.com/breakhearts/jqcli .tools/jqcli` → `conda run -n jaycode pip install -e .tools/jqcli`
- **本地大模型**：Qwen3.8-Flash（OpenAI 兼容，`.env` 的 `LLM_BASE_URL` / `LLM_API_KEY` / `LLM_MODEL`）。挖掘阶段接入，当前未启用。

## 根本问题一：聚宽研究环境能提供什么（能力地图）

价值 = 独有性 × 可获得性 − 风险。按证据强度分档，**只有"实测"档可建关键路径**（R4-9）。

**A. 实测确认可用**（一手证据）：

| 能力 | 实测边界 |
|---|---|
| `get_price` 日线 | 2005-01-04 起 5528 只（含 264 退市）；单次 33 万行/6.6s/27.5MB；`fq=None/post` 可用 |
| `get_call_auction` 竞价 | 单次 5000 行上限 → 按 2500 只分组 |
| `get_all_trade_days` | 5263 天已落盘（含未来占位日，落盘截断） |
| `get_all_securities` | 含上市/退市日期 → 幸存者偏差免疫 |
| download 通道 | 1.5MB/s，批量落盘主路（redis 中转实测慢 6 倍，弃） |

**B. 实测确认（2026-09-02 B 档探针，第二轮）**：`get_valuation`（日频全市场 5208 行，pe/pb/总市值/流通市值，单位亿元）、`get_money_flow`（主力/超大/大/中/小单净额与占比，无空值）、`get_mtss`（两融 9 列）、`get_extras('is_st')`、`get_billboard_list`（当日全市场 809 行）、`get_industry(date=)`（**行业 PIT 时变性实证**：中国石油 2010"采掘I"→2026"石油石化I"）、`get_concepts`/`get_concept_stocks`（399 概念）、`finance.STK_INCOME_STATEMENT`（**`pub_date`/`report_date` 字段实测存在**，经 `finance.run_query` 查询）。

**C. 确认不可用/不可信**：`factor` 复权因子（实测恒 1）、`get_split_ratio`（研究环境不存在该函数）、前复权（官方自认未来函数）、2005 年前行情、长历史分钟线、`jqdatasdk`（云端未装，只能用 `from jqdata import *`）。

**硬约束（架构边界）**：远端执行默认 120s 超时（`--execution-timeout` 显式放宽）；Cookie 会被平台回收（`JqAuthError` fail-fast，更新 `.env` 后重跑续传）；官方研究环境限速 500Kbps 且禁批量导出用途 → **取数一次性成批、克制低频**，不得当数据服务。

**决策**：历史行情主渠道全面转聚宽，起点 2005-01-04。旧 chenditc/AKShare 数据已删（备份下载地址见下文附录）。

## 根本问题二：需要什么特征、怎么存、怎么更新

一个字段成为特征须过三关：**① PIT 可考**（T 日可知，不看未来）**② 有增量信息**（本地字段换算不出）**③ 评估引擎能吃**。不过关的字段越多越毒——"越多越好"以此为闸门。

### 总体架构（四层，职责单向流动）

```
L1 raw 层    data/raw/jq/**          聚宽 CSV 分片，只追加，聚宽=唯一主源
L2 特征层    data/derived/features/  宽表 parquet（date×code×特征），幂等重建
L3 评估层    vectorbt + IC/IR + IS/OOS 切分，结果回喂因子库(duckdb/parquet)
L4 挖掘层    LLM 假设器（白名单 pandas 表达式）← 失败案例回喂
更新机制：fetch 重跑（断点续传）→ build 重跑（幂等）——无调度系统，一条命令即"定期更新"
```

**引擎定案（2026-09-02）**：评估/回测走 **vectorbt + 特征宽表**，不建 qlib bin——无二进制格式坑、pandas 全表达力（LLM 强项）、vectorbt 向量化原生支持 GPU（本机有显卡）、parquet 增量更新容易。qlib 降为可选导出格式，不进关键路径。老实现（qlib 表达式引擎等）在 git `42fc087`，仅参考不复用。

**数据源纪律**：聚宽证明缺什么，才允许为那一样东西开第二源（AKShare/Tushare/爬虫暂时一个都不加——多一源=多一份口径仲裁噩梦，cn_data 教训）。

### 特征清单（raw 层，按更新频率分层）

| 频率 | 数据集 | 关键字段 | 位置 | 状态 |
|---|---|---|---|---|
| 一次性 | 交易日历 | trade_days | `raw/jq/trading_calendar.csv` | ✅ 5263 天已落盘 |
| 一次性 | 标的清单 | code, start/end_date, 名称 | `raw/jq/securities.csv` | 待取（daily 云端在用，但未落盘） |
| 日度 | 日线 | 真实价 OHLCV+money、`high_limit`/`low_limit`/`paused` + 后复权 OHLC | `raw/jq/daily/` | ✅ **2020-01~2026-09（29 片 774.7 万行）**；2005~2019 待补 |
| 日度 | 估值 | pe/pb/market_cap/circulating_market_cap | `raw/jq/valuation/` | ✅ 2020~2026（109 片 770.5 万行） |
| 日度 | 资金流 | net_amount_main / net_pct_l 等 12 列 | `raw/jq/money_flow/` | ✅ 2020~2026（29 片 767.4 万行） |
| 日度 | 集合竞价 | 撮合价、竞价额、买卖一档量 | `raw/jq/auction/` | ✅ 2020~2026（162 片 768.2 万行） |
| 日度 | 两融 | fin_value/sec_value/买入额 9 列 | `raw/jq/mtss/` | ✅ 2020~2026（54 片 450.9 万行） |
| 事件 | 龙虎榜 | 席位买卖明细 | `raw/jq/billboard/` | ✅ 2020~2026（27 片 124.8 万行） |
| 日度 | ST 标记 | is_st 长表（只存 True） | `raw/jq/st/` | ✅ 2020~2026（25.4 万行） |
| 季度 | 概念成分 PIT | get_concept_stocks(399 概念) | `raw/jq/concept/` | ✅ 2020~2026（28 快照 92.7 万行；**成分确随时变**：半导体 2025-01=99 只 vs 2026-08=159 只） |
| 季度 | 行业 PIT | sw_l1/l2、jq_l1、zjw 代码+名称（季度末快照） | `raw/jq/industry/` | ✅ 2020~2026（28 快照 13 万行） |
| 事件 | 财务公告 | pub_date / report_date / end_date + 营收/净利/成本/EPS | `raw/jq/finance/` | ✅ 2019Q1~2026Q2（30 期 27.4 万行；`run_query` 无 statDate，用 filter(end_date=)；**累计口径**经茅台核对确证） |
| 事件 | 资产负债表 | pub_date/end_date + 总资产/负债/权益/流动资产负债/货币(cash_equivalents)/应收/存货/固定资产/商誉 | `raw/jq/finance_bs/` | ✅ 2019Q1~2026Q2（30 期；字段名以云端 dir() 探针为准） |
| 事件 | 现金流量表 | pub_date/end_date + 经营/投资/筹资净额 | `raw/jq/finance_cf/` | ✅ 2019Q1~2026Q2（30 期） |

**取数实测边界（写码必守）**：`get_valuation` 单次约 **1 万行上限**（静默截断！须逐日查询）；`get_money_flow` 无此限；**估值数据 T 日盘前不可得**（T 当天行全 NaN，T+1 生成）→ 增量更新的 end 应取 T-1，L2 对 NaN 行做 drop。

### 复权口径（实测决策，搞错则全量作废）

- `factor` 字段恒 1 → 无法自行换算复权，必须直接向聚宽取；
- 默认 `fq='pre'` 是未来函数（官方原话：使用未来复权因子，回测价格不正确）；
- **raw 存两套**：`fq=None` 真实价（涨跌停/停牌仅此口径有意义）+ `fq='post'` 后复权（历史不被未来分红改写）。**不存前复权。**

### 存储与更新（最简机制）

- **存储两层**：raw = CSV 分片，文件名含日期区间，天然幂等；derived = qlib bin（表达式引擎后端），挖掘阶段从 raw 重建，raw 永不由 derived 反向覆盖。
- **定时更新 = 重跑同一命令**：断点续跑保证幂等（已有片跳过、只补缺口），无需调度系统。每日增量即 `fetch daily --start <最后日期>`；Cookie 会过期决定了更新须有人在场或带告警，fail-fast + 续传即为此设计。

## 根本问题三：以后怎么自动挖掘因子（概要，挖掘阶段展开）

自动挖掘 = 闭环四件套，缺一即退化为随机试错：

```
假设器(LLM) → 编译器(qlib表达式) → 裁判(IC/IR + IS/OOS + 五层防御) → 记忆(因子库)
     ↑                                                          │
     └──────────── 失败案例与已有逻辑 回喂 Prompt ←──────────────┘
```

记忆层（去重防同质化、失败回喂防重复犯错）是 Loop 与试错的本质区别。四件的老实现存于 git `42fc087`（factor_miner / factor_eval / factor_lib / llm_client），届时按新特征底座重写。**当前不做**：raw 无字段可编译，提前建挖掘属投机工程。

### L4 开工前置调研：死循环与卡机的七种形态及对策（2026-09-02，文献+自身事故双重来源）

自动挖掘的失控不是"程序崩了"，而是**"程序欢快地跑着，产出全是垃圾"**。业界（AlphaAgent arXiv:2502.16789、AlphaGym/MCTS arXiv:2505.11122、Alpha SAGE arXiv:2509.25055）与我们自己的事故（LLM 断连一夜入库 291 条"非法因子"）指向同一批故障模式。**L4 实装必须逐条内建对策，缺一不开工**：

| # | 失控形态 | 表现 | 对策（写进架构，不靠自觉） |
|---|---|---|---|
| 1 | **基础设施故障伪装成业务结果** | 断连/超时的空响应被记成"非法因子"，污染记忆、误导回喂（我们真实发生过，291 条） | 异常分类学：`InfraError`（网络/认证/超时）一律 fail-fast 中止本轮，**绝不进因子库**；业务级 reject（语法错/表达式非法）才入库回喂 |
| 2 | **同质化坍塌**（mode collapse） | LLM 反复产出同族变体（换个窗口的动量），库内 300 个因子实为 3 个 | 双重去重：表达式规范化精确去重 + **截面相关 >0.9 拒收**；Prompt 注入"已有逻辑清单"；学 AlphaAgent 的 AST 子树相似度（论文实证：LLM 挖掘的最大退化就是靠既有知识产同质因子加速拥挤） |
| 3 | **指标爬升而真实力下降**（reward hacking） | Loop 专挑评估漏洞，IS 分数越挖越高，OOS 一动不动 | 裁判对挖掘端**只暴露 IS**，OOS 封存到验收才看；奖励用多维（效果/稳定/换手/多样/过拟合风险），单一 RankIC 会被钻（AlphaGym 五维奖励同理） |
| 4 | **窗口过拟合**（p-hacking） | 26 特征 × 若干算子暴力组合，总有几个碰运气显著 | 强制 IS/OOS + 时间滚动多窗验证；同族参数组（如 r_5/10/20/60）算**一个**逻辑不算四个（我们已实证 `v_corr_pv_20` 方向随窗口翻转——只凭一段窗口下结论必然被骗） |
| 5 | **生成-评估死锁** | 生成器只会出评估器能吃的，评估器只认生成器出过的，搜索空间自我收窄 | 保留"探索预算"：每 N 批强制一批自由生成（不给失败案例提示），并让**已确立结论定期重跑**（漂移哨兵，防因子库随数据更新静默失效） |
| 6 | **成本/进度失控** | 云端 Cookie 过期后整夜空转；LLM 调用量无界增长 | 每轮硬预算：最大批数、单批最长时长、**连续 K 轮零产出即自动停机**（不是重试到底）；进度必须落 `signals/` 日志可查 |
| 7 | **静默卡死** | 进程活着、日志不涨 | 心跳线：每轮写 round 时间戳，外部一条 `stats` 命令可查最后活动时间；无人值守过夜前先小规模试跑一轮 |

> 主要矛盾排序：**先堵 #1（我们已真金白银踩过），再堵 #2/#3（业界共识的头号退化源）**，#4-7 实装时按表逐条对照验收。GFlowNet/RL 生成器（Alpha SAGE 等）是 L4 之后的增强选项，不属当前路线。

**批判式吸收聚宽 jqfactor**：`analyze_factor` 与我们的评估链重叠；可吸收 `neutralize(how=[行业,size,beta])` 多风险中性化与 `max_loss` 有效率门槛；**须警惕**其默认 `weight_method='mktcap'` + 5 分位 + `periods=[1,5,10]` 组合易给乐观结论，且不提供样本内外切分——评估以自己链路为准。

## 附录：已删除数据的下载地址（备查，不再使用）

- **chenditc/investment_data qlib 全市场日线包**（曾用于 `data/cn_data`，846MB / 6166 只 / 2000-01~2026-08）：
  仓库 <https://github.com/chenditc/investment_data>，发布页 <https://github.com/chenditc/investment_data/releases>（取最新 release 的 `qlib_bin.tar.gz`，解压得 `calendars/features/instruments`）。弃用原因：不更新、与聚宽口径不可仲裁。
- **AKShare 申万行业映射/指数**（曾用于 `data/industry_map`）：`pip install akshare`，接口 `ak.sw_index_first_info()` / `ak.index_hist_sw()`。弃用原因：上游源不稳定（券商评测稳定性 2.41/5），行业改走聚宽 `get_industry(date=)` PIT。


## 引擎模块（`research/`，25 支全景图，2026-09-17 梳理）

**一条流水线看懂全部模块**（数据自上而下流，判决层在下游）：

```
取数       jq_channel → fetch / fetch_alpha / jq_refresh（认证自愈）
             ↓ raw 落盘（断点续跑）
质检       check（体检门，取数后必跑）
             ↓
特征       build（日频宽表）→ build_min（分钟派生特征，P1 战役）
             ↓
因子生产   alpha（DSL 编译器）← dig（LLM 自动挖掘，配 dig_supervisor.sh/dig_health.sh）
           factorlib（账本）  alpha_board（官方榜对表）
             ↓
判决       eval（IC 裁判）→ backtest（组合裁判）→ wfo（滚动验证）
           wfo_screen（批量清洗）→ famcorr（血缘矩阵）→ sentinel（退火哨兵）
             ↓
组合/产品  ml_combo（ML 组合器 lgb/xgb）→ signals（每日荐股）
           synth（等权合成器）/ timing（择时层，预注册件）
日常       update（一键日更）
```

| 模块 | 职责 |
|---|---|
| `config.py` | 路径统一读取：`raw_dir()` / `derived_dir()` / `jqcli_bin()` |
| `jq_channel.py` | 聚宽云端通道：jqcli 三段式（exec→download→rm）、分片、认证 fail-fast、交易日历、**通道独占锁**（并发撞云端临时内核的机制化防御）、**exec 云端报错原样上抛**（否则下游只见 download not_found） |
| `fetch.py` | 取数任务：`calendar` / `probe` / `daily` / `auction` / `valuation` / `money_flow` / `industry` / `concept` / `finance` / `mtss` / `billboard` / `st`，断点续跑 + 落盘覆盖核对 |
| `build.py` | **L2 特征层**：raw → `derived/features.parquet`（26 列：动量/量能/估值/资金流/竞价失衡/两融占比/龙虎榜/ST + `fwd_ret_5` 标签；停牌与估值 NaN 不填充；两融 T+1 滞后、龙虎榜次日生效）+ `industry/concept/finance.parquet` 维表；幂等可重建 |
| `check.py` | **数据体检门**：完整性/日历对齐/跨数据集主键与语义校验 + 交易规则对抗审计（时变涨跌停交易所口径、价格带、额量价、资金流守恒）+ 增强件 join 率与自洽校验；每次增量取数后必跑 |
| `eval.py` | **L3 评估裁判**：截面 IC/RankIC/ICIR/正率 + 五分层多空价差 + 行业中性（日×行业中位去均值）+ IS/OOS 双段（2024-01-01 切）；内建可交易性过滤（剔停牌/ST/涨停触板）与截面厚度门；`--json` 机器可读输出（L4 契约）；因子列可来自宽表或 alpha 产物（按名自动 join） |
| `backtest.py` | **L3 组合级回测（vectorbt）**：`from_orders(targetpercent, cash_sharing)` 单组合 Top-K 轮动、可调调仓周期（默认 5 日）、双边费率 0.13%、净值用后复权；支持 `--reverse` 选方向；输出年化/回撤/Sharpe/Calmar/费用占比/日均持仓 + 全市场等权基准 + IS-OOS 分段 |
| `fetch_alpha.py` | 官方 alpha101 基线取数（`jqfactor.get_all_alpha_values`，每 5 交易日采样，= "对答案"标准答案库） |
| `alpha.py` | **L4 表达式编译器**（工具契约层）：白名单 DSL（列名 + 算术 + rank 截面 + delay/delta/ts_* 时序 + log/abs/sign）经 AST 校验后执行——LLM 产出物唯一的入口，杜绝任意代码执行与未来函数；产物 `derived/alpha/<name>.parquet`（date/code/value），与宽表列同契约进 eval |
| `factorlib.py` | **私有因子库机器账本**（`derived/factorlib.json`）：档案（表达式/假设/eval 摘要/组合成绩/状态）+ 状态机（candidate→product→retired/rejected，非法流转拒绝）+ 去重索引（exprs 子命令供假设器查重）；FACTORS.md 是它的人读镜像 |
| `sentinel.py` | **淘汰哨兵**：在库 candidate/product 因子用最近 250 交易日复算中性 RankIC/ICIR，方向翻转判 die、强度跌破地板判 decay，`--apply` 自动退役并记录原因；有事非零退出供上层感知 |
| `dig.py` | **L4 自动挖掘回路**：propose→compile→judge→memorize 全自动闭环，只在"提假设"节点调本地 Qwen；七形态对策逐条落码（InfraError 中止整轮、秩相关>0.9 换皮拒收、只回喂 IS 数字、机制连败拉黑、硬预算停机、dig_log.jsonl 心跳）；2026-09-17 起 prompt 内嵌"深度思考四步法"、MAX_TOKENS=32000。配套 shell：`dig_supervisor.sh`（崩了拉起）/ `dig_health.sh`（心跳体检） |
| `jq_refresh.py` | 聚宽 Cookie 自动续期：jqcli `auth login` 用户名密码重登，Cookie 回写 .env（治"研究平台会话被回收"老毛病，取数链自愈环节） |
| `build_min.py` | **分钟聚合→日内日频特征**（P1 风向标战役）：`raw/jq/min_agg/` 云端聚合的开盘/下午窗口量价 → alpha 契约长表（m_gap3/m_pull30/m_vconc3 等窗口内自洽比值，无未来函数），eval/backtest/wfo 按名自动加载 |
| `alpha_board.py` | alpha101 官方基线榜单（对答案参照系）：`raw/jq/alpha_ref/` 324 采样日 82 因子，与 eval 完全同口径，按 |IS ICIR| 排序，产出 `derived/alpha_board.csv` |
| `wfo.py` | **WFO 驱动器（验证协议 v2 执行者）**：`years`=单因子逐年成绩（回答"是不是只在某几年灵"）；`rotate`=滚动年度选枪（每年只用过去的数据从池里选 Sharpe 最高者），预注册及格线写死在本文件（逐年 Sharpe>0 ≥4/6、非单年贡献过半） |
| `wfo_screen.py` | 候选大清洗：全部 candidate 批量过 WFO 同一道门（治 dig 的 IS 线在 7×24 轰炸下失效的候选通胀），只判决不改状态，人工过目后 `factorlib set-status` 执行 |
| `famcorr.py` | 候选家族血缘矩阵：全体 candidate+product 日截面秩相关（逐对跨日最大 |Spearman|），量出每支枪与最近血缘/与在役产品的重复度，供"留谁并谁"决策，产出 `derived/famcorr.json` |
| `ml_combo.py` | **ML 因子组合器**：全体在册因子日截面 rank 进特征池、fwd_ret_5 rank 做标签，逐年向前（隔离带防泄漏）合成信号塞回 WFO 同口径回测；`--model lgb|xgb` 双裁判（2026-09-17 XGB 同池终裁获胜，RankIC 0.1166/同窗 +15.9%/0.71）；产物 `derived/alpha/mlb_*.parquet` 走 alpha 契约 |
| `synth.py` | 多因子合成器（打分层基线）：成员逐日截面 rank→方向对齐→z-score→按族权重求和（"|"分族防同原料刷分），产物写 alpha 契约可被 backtest/eval/wfo 当普通因子直接用 |
| `timing.py` | 仓位择时层（预注册 2026-09-08）：等权指数>MA(N) 满仓否则降档，PIT shift(1)、网格 N∈{20,60,120}×off∈{0,0.5}、只看 IS 选优——现状：判关闭候复议 |
| `signals.py` | **每日荐股层（产品二）**：直接调 `backtest.topk_weights`（与回测同一实现），持仓=数据纯函数，输出应持仓清单+买卖动作；`build(col,...,skip_refresh=True)` 供 ML 合成信号（无表达式）跳过重编译 |
| `update.py` | **一键日更**：刷日历→各数据集按分片文件名解析缺口只补增量（幂等、分片只追加）→三档出数节奏（close 档 `CLOSE_HOUR` 晚 8 点门槛，治"盘中快照当收盘"事故）；夜间链 `data/derived/nightly.sh` v2 串 update→check→build→荐股 |
| `jqfa_check.py` | 对表体检：项目内隔离 venv（`.tools/jqfa-venv`）跑聚宽同款开源 jqfactor_analyzer，与我方 eval 指标对比盖章；全程本地、不上传（规则 8） |

`__init__.py` 为包标记。其余零散 py：`data/derived/` 下 20 支（absorb2 / band_retrial2 / subtraction2 / csi500_test / pm_probe / probe_* / xgb_report / gen_factor_doc 等）是**一次性战役执行器**——每支对应一场预注册判决的现场代码（收编、band 重审、减法、宇宙缩窄、涨停生态、下午段侦察……），判决结论已归档 PROJECT.md，脚本本身作为判决档案配套保留不删、不入库（data/ 区 gitignore）。日常只需认识上表 24 支。

评估（`factor_eval`）、因子库（`factor_lib`）、挖掘（`factor_miner`）、LLM 客户端（`llm_client`）等模块属于后续阶段，已在 git `42fc087` 保留，待特征底座成型后按需重写，不提前搬回。

```
# 第一阶段主命令（按顺序）
conda run -n jaycode python -m research.fetch calendar    # 最先：交易日历（已完成）
conda run -n jaycode python -m research.fetch probe       # 通道与体积探针
conda run -n jaycode python -m research.fetch daily       # 全市场日线（真实价+后复权，默认 2025 起）
conda run -n jaycode python -m research.fetch valuation   # 日频估值（逐日查询，15 天/片）
conda run -n jaycode python -m research.fetch money_flow  # 日频资金流
conda run -n jaycode python -m research.fetch industry --start 2025 --end 2026   # 行业 PIT 季快照
conda run -n jaycode python -m research.fetch concept --start 2025 --end 2026   # 概念成分 PIT 季快照
conda run -n jaycode python -m research.fetch finance --start 2024 --end 2026   # 财务公告（按报告期）
conda run -n jaycode python -m research.fetch auction --start 2010-01-01         # 竞价
conda run -n jaycode python -m research.build                                    # L2 宽表（raw 更新后重跑）
conda run -n jaycode python -m research.check                                    # 数据体检门
```

## 进度与下一步

**已完成**
- ✅ 通道打通与机制定型：云端执行 → `jq_out/` → download 本地 → 即删云端（含 `--execution-timeout` 显式放宽，默认仅 120s）
- ✅ 主渠道可行性定案 + 复权口径实测决策（见上）+ 历史深度定界（2005 起）
- ✅ 环境合并：jqcli 装入 conda `jaycode` 单环境，`.tools/venv-jqcli` 已删除
- ✅ 交易日历落盘并读回核对（5263 天，2005-01-04 ~ 2026-09-02）
- ✅ **日线小规模首战**：2025-01~2026-09 共 8 片 209.6 万行落盘；读回验证通过（行数≈理论值、涨跌停价=前收×1.1 精确、后复权比值随分红递增、双口径 NaN 一致）。实测单片（60 交易日×全市场）约 33MB/35s，比预估快一个量级
- ✅ 项目聚焦重构：只留取数引擎 + `docs/jq/`；旧数据/旧因子代码全部下线（git `42fc087` 可查）
- ✅ **多维取齐 2025~今**：估值（208 万行，修复 1 万行静默截断）、资金流（208 万行）、行业 PIT、概念 PIT（34.9 万行）、财务公告含 pub_date（8.3 万行）——全部过 `check.py` 体检门（含交易规则对抗审计：涨跌停交易所口径、价格带、额量价、资金流守恒，全绿）
- ✅ **L2 特征层建成**：`build.py` → `features.parquet` 209.6 万行 × 15 特征 + `fwd_ret_5` 标签；可用样本 196 万；茅台手工复算与宽表分毫不差
- ✅ **数据大扩张完成（2026-09-02）**：全部 10 个数据集补齐至 **2020-01~2026-09（6.7 年）**，raw 层共 **~4300 万行 / 约 4G**（日线 775 万、估值 771 万、资金流 767 万、竞价 768 万、两融 451 万、龙虎榜 125 万、ST 25 万、概念 93 万、行业 13 万、财务 24 万）。全区间过 `check.py` 体检门全绿（含时变涨跌停规则：创业板 2020-08-24 起 20%）；三个疑点均已定性（资金流缺"今天"=T+1 出数、茅台 10 差异日=除息日、标的数增长=市场扩容非跑漏）。**空间结论：2020~今全量 ~5G；补 2005~2019 约 12G——空间永不是约束**
- ✅ **生产化规划定调（详细设计待 L3 后）**：三层存储 raw→derived→`signals/`（每日选股信号，只追加）；更新=一条命令（fetch 增量→check 门→build→打分），无守护进程；选股口径统一"截至 T-1 完整数据选 T 日股"（估值/资金流 T+1 出数，天然 PIT 安全）；外部比对仅用 AKShare 月度锚点日 + 季度 --force 历史片 diff
- ✅ **分钟/tick 定位结论**：tick 历史聚宽本就没有（仅近期快照），永久搁置；分钟 K 历史有限且原始全量入库（~200GB/几十小时在线）与通道形态不匹配——**正确用法是将来"云端聚合、只取日频统计结果"**（尾盘动量/日内波动结构/量分布等），列为 L3 撞 IC 天花板后的预备役
- ✅ **对答案战役第一轮（数据可信度第三方互证）**：挖到官方标准答案接口 `get_all_alpha_values(date,'101')`（一次返回全市场当日 101 个 WorldQuant Alpha，实测 2020-01 起、非全 NaN 因子 82 个）。从自家 raw 后复权数据复算 alpha_002/003/006/012 与官方逐票对比：**alpha_002/006 Pearson 0.999+、alpha_003/012 Spearman 0.92+**——证明我们的数据与官方管线用同一份事实（价格/量/复权口径全对），同时给 eval 裁判盖章。`fetch_alpha.py` 按 5 日采样取全历史 324 天，作 LLM 因子的免费基线榜单
- ✅ **vectorbt 装上**（1.1.0，开源版；坑：plotly≥6 删 `scattermapbox` 致导入崩，已锁 `plotly<6`）；`backtest.py` Top-K 轮动 + 费率 + IS/OOS 净值
- ✅ **组合回测首仗打完（vectorbt 框架验收 + 首个真钱成绩单）**：框架自证——单票买入持有 vbt vs 手算误差 2e-16；`from_orders(targetpercent, cash_sharing=True)` 是组合正确姿势（from_signals 多列各分 init_cash，口径失真）。`v_amt_5_20` Top-100 五日轮动：无费年化 **+3.5%**、含费 **−6.4%**（费率 6.7 年吃掉初始资金 51%）；反向做多最大端年化 −46.6%——方向与 eval 完全一致，框架无偏。**主矛盾浮出：换手×费率吞掉信号**（详见研究结论 6）
- ✅ **alpha101 官方基线库建成（对答案弹药库）**：全历史取数完成 **33/33 片、324 采样日、154.4 万行、1.2G**（`raw/jq/alpha_ref/`，列 day/code/alpha_001..101）。途中两次失败均定性修复：并发误判（真因=jqcli exec 报错被吞）、`%%` 转义笔误
- ✅ **`update.py` 每日增量更新**（自动补到最新、T+1 分档、幂等；待次日用真实缺口实弹验证一次）
- ✅ **通道两次事故机制化**：①jqcli 云端 exec 报错时 returncode 仍为 0，错误被吞、下游只见 download `not_found`（曾误判为并发冲突）→ `run_script` 现检查 exec 返回 `status=error` 并原样上抛；②同通道并发跑两个取数进程会撞云端临时内核 → `_acquire_channel_lock()` O_EXCL 独占锁（含陈旧锁清理），把"纪律 5"从自觉变机制

## 验证协议 v2（2026-09-07 定稿，替代静态 IS/OOS）

**背景**：原协议 2024-01-01 静态切分，OOS 本应"只看一次"，实际已被消费至少三遍（结论9自曝、结论12/13 展示与判决）——**看过的考卷即沦为 train，不再是证据**。且 2020~2026 只有一份考卷，暂无更多历史可切。

**新规**：
1. 2020~2026-09 **全段转为研究样本**（诚实承认：此段的"OOS 成绩"均是历史参考，不得再当无污染证据引用）；
2. 决策依据改**滚动 WFO（walk-forward）**：在窗口 N 调参 → 只在窗口 N+1 验收一次 → 窗口整体前滚；每次验收时该窗口是"做决定时尚未发生的未来"；
3. **新鲜考卷由时间生产**：每日 update 落盘后，最新一段数据天然是所有历史决策的样本外——update 机制是本协议的基础设施；
4. LLM 挖掘时代沿用 L4 七形态表 #3：裁判对挖掘端只暴露当前 IS 窗口，验收窗口封存。

 ### L4 框架选型初步结论（2026-09-07 调研 + 09-08 deepseek-harness 补充，定案等彩排）

2026 年 Agent 框架竞争焦点已收敛到**状态治理/故障恢复/可观测**——恰是七形态表 #6/#7（成本失控、静默卡死）的解药，与本项目需求精确对位。硬约束过滤：本地 Qwen（OpenAI 兼容）+ R4-8 零外泄 → 云端 tracing（LangSmith 云）与云绑定框架（ADK/Bedrock）出局；AutoGen 官方已入维护模式，新项目排除；CrewAI/smolagents 偏"角色链/代码生成"自主范式，与我们"流程代码写死、只在假设器节点调 LLM"的确定性闭环不匹配。

**deepseek-harness（dsh，2026-08 开源）**"一切皆插件"对本项目的正确读法：**能力活在插件里，壳会换代、工具插件才是资产**。其 headless 模式（一条命令跑完退出）、追加式全量留痕（可回放/检索，对 #7）、workspace-write 权限锁（对 R4-8）三点与我方需求精确对口；但官方明示开发者预览版将有 breaking changes 且为 Node/TS 栈——**当实验台试用，主线不押上**。

**分层定案（资产/壳分离）**：
- **资产层（已建成，2026-09-08 工具契约层）**：`alpha.py`（白名单 DSL 编译器=LLM 产出唯一入口）+ `eval --json`（裁判机器可读）+ `factorlib.json`（记忆/状态机/去重）+ `sentinel.py`（淘汰）+ update/build/check（数据底座）——统一命令行契约（参数入、JSON 出、错误非零退出），任何壳都按同一契约调用。
- **壳层（可替换）**：候选 = LangGraph（Python 原生 checkpoint）与 dsh（headless/留痕）；先用"人肉彩排"把 `propose→compile→judge→memorize` 函数跑实，再把函数图化挂壳；实装前补验本地 Qwen function-calling 质量。

**下一步**
0. ~~财务特征首测~~ ✅ **2026-09-08 判决（结论15）**：cash_quality 入候选（neu ICIR 0.289 稳定），财务面整体弱——主战场仍在量价/事件
1. ~~update.py 真实缺口实弹~~ ✅ **2026-09-07 首战全胜**：新 Cookie 下补 09-03~09-07 缺口（七数据集）→ check 体检门全绿 → 宽表重建至 2026-09-07（776 万行/1620 天/可用样本 758 万）。"断点续跑=更新机制"实战闭环成立
2. ~~财务三表回填~~ ✅ **bs+cf+inc 三表 2019Q1~2026Q2 共 90 期落盘**，并已建成 **9 个 PIT 财务特征**入宽表（`fin_roe_ttm/fin_gross/fin_opm/fin_rev_yoy/fin_np_yoy/fin_cash_quality/fin_debt/fin_cash_asset/fin_goodwill_eq`）。口径：累计值→TTM（手工对答案：茅台 51.80bn 精确命中、ROE 30.9%、毛利率 91.4%）；更正披露取首版；PIT 生效日=三表 pub_date 取晚。check 新增"九、财务三表体检"：pub_date 零缺失、**BS 恒等式 24.28 万行仅 10 违例**、宽表覆盖率 99%。**教训入则**：期间平移方向是未来函数高发区——首版把源行回拨=取到未来值（茅台 ROE 为负当场暴露），已注释警示；`DateOffset` 月份滚动须 MonthEnd 吸附（09-30+3m=12-30 坑）。
3. **现货约束下的弹药重验收**：三强候选（v_amt/auc/vlm）按验证协议 v2 跑滚动 WFO + 绝对收益/回撤口径（基准换沪深300/中证500），产出可执行域内的第一个"产品"
4. 因子库档案已建：**FACTORS.md**（三候选+两封存+一回喂原料，L4 因子库表的结构蓝本）
5. L4 自动化挖掘（LLM 假设器为目标形态，先以"人肉闭环"彩排铺轨；开工前过七形态对策表）
6. 择机：仓位择时研究（无融券世界唯一对冲通道）、2005~2019 历史补齐、`signals/` 生产层（荐股机器底座）

## 已确立的研究结论

0. **L3 基线开考（2026-09-02，2020~2026 全区间、可交易样本 732 万行，标签 fwd_ret_5）**——管线验收通过：教科书异象在 A 股全部复现且 OOS 不衰减：反转 `r_20d`（OOS RankIC -0.065）、**放量负向 `v_amt_5_20`**（OOS -0.045，ICIR 中性后 -0.52，用户核心信念第三次复现）、低波动 `v_std_20`（-0.078）、低换手 `vlm_turnover`（-0.075，多空价差 -45bp/5日）、小市值 `vlm_ln_mv`（-0.035）。**新发现**：竞价额占全日比 `auc_money_share` 负向且中性化后 ICIR -0.71（竞价维度的第一个信号）；龙虎榜次日 `bb_yest` 显著负（RankIC -0.045）；主力资金流 `mf_net_pct_main` 五日 IC 很弱（-0.01）——"跟主力"在周频尺度不成立。**证伪警示**：`v_corr_pv_20` 在 2023~2026 旧窗口 RankICIR +0.82，放到 2020~2026 全区间翻负（-0.55）——**因子方向随市场窗口翻转，这就是必须全历史 + IS/OOS 的铁证**，任何只在近三年验证过的结论都不可信。

1. **量能 > 价格**：放量类是最强量价方向且为**负向**（放量→未来收益低），近 3 年震荡市成立（多轮交叉印证）。
2. **行业是分层不是信号层**：行业自身动量弱（RankICIR 0.1~0.2）；个股因子做行业相对化/中性化后信号更纯净（0.50→0.58）。
3. **集合竞价撮合价 = 开盘价**（复权后完全相等）→ 竞价的增量信息不在价格，而在竞价额、买卖五档失衡。
4. **单因子 IC 绝对值普遍 < 0.03**，须多因子合成；IC 数字不可当实盘预期。
5. LLM 可稳定产出 qlib 表达式因子，但须约束：不支持一元负号（写 `0 - X`）、不支持条件 Rank、不支持跨标的引用。
6. **组合层实测（vectorbt，2026-09-02，Top-100 五日轮动、双边 0.13%）**：①`v_amt_5_20` 十分位 fwd_ret_5 年化呈单调递减但**深端反转**（D1~D4 段 16~20% 最高、D9 端 −13.6%、D0 端仅 12.7%）——IC 负向≠做多最小端，**信号集中在"避开高放量端"而非"买入最低端"**，最低端是小票/流动性陷阱；②框架验证无误（buy&hold 误差 2e-16、方向语义与 eval 一致）后，真钱成绩光板：无费 +3.5%/年、含费 −6.4%/年——**周频全换手 Top-K 在 A 股费率下不可行，降换手（缓冲带/长周期/合成信号）是组合层的第一个研究课题**；③基准口径陷阱：`ret.where(tradable)` 会把复牌跳空集中记入复牌日、系统性压低基准年化（+10.0% → −22.7%），组合基准一律用全票等权。
7. **工具链分工实测（2026-09-02，1617×5438 矩阵）**：因子信号评估（IC/分层）用纯 pandas（eval.py，秒级）；组合级（持仓/费率/换手/净值）用 vectorbt。同口径对账：vbt 与独立 pandas 复算日收益相关 **0.9996**、终值 1.170 vs 1.175（微差来自 pandas 版"调仓间权重不漂移"近似）——两边都对、结论一致。**vbt 的价值不在单次速度**（单次模拟 50s，反而慢于 pandas 向量化 0.4s），而在账户/订单/费用语义的正确性与参数扫描广播并行；手写账户两天踩三个坑（init_cash 分摊、bool 退化、复牌跳空）就是反面教材。

8. **降换手战役开战调研（2026-09-03，全部一手实测复现）**：①信号与基线复现无误（eval：OOS/neu ICIR -0.520；backtest rebal=5 含费年化 -6.4%、费率占比 51.2%）。②**调仓周期敏感性首测（K=100）**：rebal 5/10/20 日 → 含费年化 -6.4% / -0.2% / +2.5%（Sharpe -0.11 / 0.13 / 0.23，费用占比 51% / 36% / 21.5%）——**单纯拉长周期能把费后从深负拉回微正，但远达不到 Sharpe>0.5 及格线，且无费毛 Sharpe 也只有 0.26~0.36**，说明矛盾不止换手：多头 Top-K 绝对收益本身大幅跑输等权基准（毛 +3.5~6.3% vs 基准 +10.0%）。③**最低端流动性陷阱实锤（十分位结构直查）**：D0（最低放量端）平均市值 56.6亿/日换手 1.84% 为全场最低，5 日均收益 +24.4bp < D1~D4 的 +35.1bp；D9 端 -26.2bp（换手 5.0%）——"避开 D9"是信号主体、"买入 D0"是低质小票，与结论 6① 互证；最优多头区间为 **D1~D4 带（band）**。④**规划含义**：仗要分两层打——组合工程层（buffer 缓冲带 + band 选股区间 + 长周期）先榨干单因子；若仍不及格，问题在"纯多头吃市场贝塔"，转信号中性化/多空或合成路线。

9. **降换手战役·第一层战报（2026-09-03，backtest.py 增 buffer/band 后 8+4 组矩阵）**：①**band 是决定性武器**：只在截面名次分位带 (0.10,0.50] 选股（跳过 D0 微票陷阱带）+ rebal=10 → **含费年化 +8.8%、Sharpe 0.46**（IS 0.40 / OOS 0.54），较原基线（-6.4%/-0.11）天壤之别——**组合收益的主驱动是"避开微票带"的贝塔/质量暴露选择，不是信号精修**。②**buffer 缓冲带无效甚至微负**：带内成员每期重排，留存语义落空，费用仅降 0.8pp 而收益不增——该机制对"带内重排型"信号无用，弃。③**诚实账**：最优组超额 vs 全市场等权基准（+10.0%）仍约 **-1.2pp/年**——A股微票在等权基准里贡献巨大，避开微票=放弃这块贝塔红利；本组合本质是"优质带贝塔 + 弱负向信号微调"，**纯 alpha 未证实**。④**费后换手仍是拦路虎**：rebal=10 费用占比 53%、rebal=20 降至 27% 而 Sharpe 几乎不掉（0.46→0.45）——降费应走"带内低换手化"（信号平滑/EMA、带边界加宽）而非 buffer。⑤战役判定：**未过 Sharpe>0.5 及格线（全期 0.46 贴线）但大幅接近**；转第二层：行业中性选股（剥离行业贝塔测真 alpha）+ 多因子合成，同时用 EMA 信号平滑再压换手。**流程违规自曝**：网格扫描时把 OOS 数字打印了出来（本想只看 IS）——本次决策未用 OOS（IS 排序与最终选择一致、未因 OOS 改参），但下次网格必须物理屏蔽 OOS 输出。

10. **降换手战役·第二层战报（2026-09-03，backtest 增 neutral/smooth 机制，等价回归全过）**：①**基准 Sharpe 实测 = 战役标尺修正**：全市场等权基准自身年化 +10.0%、**Sharpe 0.56**（IS 0.50 / OOS 0.64，2020~2026 是 A股微票牛市）——**"费后 Sharpe>0.5"及格线定错了**，它低于基准；多头组合的及格线必须改用**超额口径**（vs 基准的信息比率/超额年化），绝对 Sharpe 在单边市场上无意义。②**行业中性选股实测**：band+neutral（日×行业内算分位）年化 +5.1%、Sharpe 0.33、费 45.1%——**比不中性化（+8.8%/0.46）更差**，说明 band 收益里行业暴露是正贡献（信号偏好的低放量票恰处强势行业），剥掉行业反而剥离了收益来源。③**分位平滑 smooth=10 实测**：band+smooth 年化 +8.3%、Sharpe 0.44、费 53%→47.3%——**方向对但力度不够**，费用降幅不及预期（滚动均值对日频噪声的平滑窗口仍短，且带内重排照旧发生）。④**第二层判决（如实）**：多头路线三种武器（band/neutral/smooth）全打满，最优仍 +8.8%/0.46 < 基准 +10.0%/0.56——**v_amt_5_20 的多头端不产生超越市场的 alpha，信号的钱在"排除 D9"（空头/负向侧），long-only 结构吃不到**，与结论 6①"信号集中在避开高放量端"完全自洽。⑤**路线含义**：继续在 long-only 上堆机制是添油战术；下仗二选一——(a) **超额口径重构评估**：组合改算 vs 基准的超额与信息比率，直接回答"信号有没有 alpha"；(b) **多空/对冲结构**：做多 band + 做空 D9 端（融券约束下先做纸面对冲口径），让负向信号的钱真正落袋。

11. **超额口径判决（2026-09-04，backtest 增 超额年化/TE/IR/贝塔/相关 + IS/OOS 分段 IR）**：事前立标准（IS 段判：超额年化>0 且 IR>0.3 → 有 alpha），跑 5 组 IS 后指定唯一候选（band(0.10,0.50] rebal=10），OOS 只看一次。**结果：IS IR +0.02、OOS IR -0.07、全期超额年化 -0.4%、IR -0.01——long-only 组合层面无 alpha，判决成立**（这不是失败，是排除法胜利：彻底关闭"多头堆机制"路线）。诊断细节：①**贝塔 1.15 / 相关 0.96**——band 组合本质是"1.15 倍杠杆的等权市场"，绝对收益差异全是贝塔放大器效应，与 alpha 无关；band 的作用是把组合从 D0/D9 两个极端暴露拉回"市场中部"，所以它显著改善绝对收益（-6.4%→+8.8%）却不产生超额。②**alpha 在截面两端之间，不在多头一端**：IC/分层证据显示价差 = "避开 D9"(-26.2bp/5d) + "持有 D1~D4"(+35bp/5d)，多头 band 只吃到后者且被前者的市场拖累抵消（放弃 D9 的同时基准里 D9 在涨）；唯一能同时吃两端的结构是**多空对冲**：多 band + 空 D9，市场贝塔归零、纯截面价差落袋——**下一仗（(b) 纸面对冲口径）已由本判决必然导出，无第二选择**。③流程纪律执行良好：本轮 OOS 输出物理屏蔽、候选事前指定、终验只看一次。

12. **多空纸面对冲大捷（2026-09-04，降换手战役终局，`run_ls` 双腿口径：多 band(0.10,0.50] + 空 D9 最高放量 10%，各 50% 资金，费率 0.13%+融券 8%/年从厚）**：**及格线（费后年化>0 且 Sharpe>0.5）以十倍余量碾过——rebal=5 费后年化 +29.1%、Sharpe 2.77、最大回撤 -9.3%；IS 2.44 / OOS 3.23（信号在 OOS 不但未衰减反而更强）；rebal=10 降换手版 +20.0%/2.08 同样碾压**。①`v_amt_5_20` 自立项以来（用户核心信念，第三次复现于 L3 基线）至此完成全链路验证：**IC 显著 → 分层定位钱在两端 → long-only 证伪 → 多空结构兑现**，成为第一个入库候选因子。②**纸面≠实盘的边界必须说破**：本口径验证的是"信号值钱"（回答是响亮的），但实盘还差三件事——融券券源可得性（D9 多为放量活跃票稍好，但政策风险在）、保证金占用与杠杆约束、冲击成本（D9 高换手端滑点大于均值）。③**诚实留痕**：首跑曾得 -2.8% 负结果，根因是空头腿 band 参数按"绝对分位"误写（band 实为"选择端分位"，选成了做空低放量端=做多有信号端反向）；靠"方向与 IC 证据矛盾"抓住——修参是**正确性修复非调参**（修复前 IS/OOS 全负，不存在挑数据），docstring 已补语义说明。**教训固化：带方向的参数必须先用小样例核对语义再上战场；对结果的第一校验是与既有证据交叉对照**。④战役总结：降换手战役全胜收官（-6.4% → 对冲 +29.1%），三判决树走"成功"分支：**转多因子合成**（v_std_20/r_20d/auc_money_share 同法验证）。

13. **多因子合成战役·判决：5 因子等权合成无增益，单因子仍是产品（2026-09-05，run_ls 同法单腿 4 因子 + 相关性审计 + 事前定池定权合成终验）**：①**单腿验证（统一口径，OOS 物理屏蔽）**：`vlm_turnover` +64.5%/IS 3.48、`auc_money_share` +39.6%/**IS 4.76 回撤仅 -5.3%**（池内质量王，与所有因子相关 <=0.25 最独立）、`r_20d` +33.8%/1.95（与 v_amt 相关 0.39 偏高）、`v_std_20` +23.8%/1.42 回撤 -23.4%（拖后腿）。**低换手/竞价占比两个独立信号被此确认，弹药库扩大**。②**合成终验（池=5 因子全入、等权 rank 均值，均事前定）**：+44.5%/Sharpe 2.66/回撤 -17.7%（IS 2.54/OOS 2.83）——**年化比单因子 +15pp，但 Sharpe 低于基线 2.77、回撤翻倍，预定及格线（IS>2.8 且回撤<=8%）双项不达标，判决：等权合成无增益**。审计排除技术嫌疑（每日 5 因子齐全票中位 4945 只，非覆盖率问题）。③**机理诊断**：等权合成把高出血腿（v_std 回撤 -23%、r_20d -25%）与低波腿（auc -5%）混成"平均暴露"，年化上升是**风险堆叠而非信号提纯**——Sharpe 不可加，合成只在"因子间低相关且单腿质量相近"时才有增益，本池内质量方差太大（IS 1.95~4.76）。④**正确教训（供下轮假设，勿直接当结论）**：合成池应"少而精"（如仅 auc+vlm+v_amt 三强）、或按 IS Sharpe 倒数配风险预算（而非等权）；**这些是新假设，须新协议预验，本役 OOS 已用掉不得回看**。当前产品决策：**私有库第一个正式产品 = v_amt_5_20 单因子多空对冲（+29.1%/2.77）**，auc/vlm 作为独立候选入库备用。

14. **alpha101 基线榜单建成（2026-09-05，`research/alpha_board.py`：82 官方因子 + 自家 5 因子同口径打分，产物 `derived/alpha_board.csv`）**：口径=5 日采样 324 天 join 可交易样本 146.6 万行 + fwd_ret_5 日截面 RankIC + IS/OOS。**发现**：①alpha101 头部全是**正向结构类**（alpha_016 IS ICIR +0.69、013/044/040 等 0.6+），与自家**负向量能类**方向完全互补——私有库与官方库无重复发明，合成素材空间打开；②自家对标：**auc_money_share IS -0.486/OOS -0.712 已入全场 Top12**（量级直逼官方头部且 OOS 更强），vlm/v_std IS -0.51~-0.55；③**IC 榜与钱榜再度分道**：产品 v_amt_5_20 的 ICIR(-0.27) 自家垫底但 run_ls Sharpe 全场最高(2.77)——IC 是必要非充分，终裁在组合（呼应结论13合成教训）；④全库 IS/OOS 方向同号率 82%，榜单时间稳定性可作 LLM 参照系。**踩坑留痕（重要）**：向量化掩码秩相关首版有错——X 按因子非空全集排秩、Y 按标签非空集排秩，两集合不一致致掩码填 0 污染统计量，产出 RankIC -0.37~-0.90 的假高值；靠"数值异常高直接红旗"抓出，与标准 pandas 实现逐日对照定位，修复=两侧均在交集内重排 + **自检 assert（向量化 vs 标准算法逐位一致）固化进脚本**。教训：**掩码统计必须两侧同集合重算；自检断言是向量化代码的标配**（同结论12教训：结果第一校验=与基准证据交叉对照）。

15. **财务面首测判决（2026-09-08，9 个 fin_ 特征过 eval，样本 734 万行）**：①**财务面整体比量价面弱一个量级**——最优 ICIR 0.29 vs 量价系 0.5+；2020~2026 A股是微票量价主导市，基本面普遍被压制。②**唯一稳定新信号=`fin_cash_quality`（经营现金流/净利，正向）**：full/neu ICIR +0.289、IS +0.197/OOS/neu +0.211 同号不衰减、pos% 0.62 全场最高、行业中性后增强——"现金为王"在 A 股截面成立，入候选。③`fin_np_yoy` 边缘信号（IS +0.072/OOS +0.132 同号、量级小）。④**教科书因子 ROE 直接失效**（IC -0.0008，IS/OOS 方向翻转）——教科书异象清单在 A 股这段再减一员，呼应"因子方向随窗口翻转"铁律。⑤含义：**财务面不是主战场**（补它的价值=正交原料+风控过滤，如剔除现金流恶化票），50% 年化的指望不能押在它身上。

16. **数据事故复盘：盘中快照当收盘（2026-09-08，用户指出）**：09-07 update 在 10:54 执行，daily/auction/billboard/st 把"当时正在进行"的 09-07 当收盘落盘——取证：close 86% 票不符终值（最大偏差 12%）、成交额仅终值一半（1.09 vs 1.95 万亿）、billboard 整天缺失（未公布被抓成空片，断点续跑按文件名跳过=**静默永久缺口**）。auction/st 无损（竞价 9:25 定盘、ST 盘前已知）。**关键教训：这类错误在文件内部完全自洽，体检门与外部硬规矩全过——数据校验的盲区是"时间维度"（齐、对，但没"完"），只能靠取数时点纪律防**。修复：拆旧片剔脏行、重取收盘终值、补龙虎榜、update.py 三档节奏+`CLOSE_HOUR=20` 收盘门槛（白天 daily/billboard 自动止于 T-1），规则入 rules 第 7 条"未收盘的数据不是数据"。

17. **人肉彩排三轮战报（2026-09-09，工具契约层全流程：propose→compile→eval→backtest→factorlib）**：产出 **第一个 product 级因子 `a_rev_x_lowvol`**（`rank(-r_20d)*rank(-v_std_20)`，band 多头含费全区间 Sharpe 0.65 > v_amt 0.46，IS 预注册过线→OOS 一次性 0.68）+ 候选 a_quiet_two（0.60，同族待 WFO 二选一）+ 拒收 a_cold_horse（IC 全场最强档但 IS 组合 0.32 即拒，OOS 未看）。**三条硬认知**（全部进 PROPOSE.md 回喂）：①**IC 是排序证据，组合是暴露证据**——"最强×最强"交互 IC 最高、组合最差（放大共同微票暴露，贝塔 1.12）；"两弱腿"交互反赢因交互把暴露结构修正归一（贝塔 0.96）；②**回测方向必须与 eval IS/neu IC 符号强制联动**（第 2 轮方向选反 IS -11.8% 差点错杀好假设，正确性修复≠调参的例外条款由此立，同结论12③）；③**预注册判决流程机器化跑通**：事前定线（IS Sharpe≥现役基线 0.46）→IS 判→过线才放 OOS 看一次，三轮零违规。

18. **WFO 滚动验证首考（2026-09-09，wfo.py：验证协议 v2 落地）**：`a_rev_x_lowvol` 逐年 2020~2026 正 Sharpe **6/7 年**（及格线 4/6），最佳单年仅贡献 23% 收益（不靠一年吃饭）；最差 2022 熊市 -5.0%（同年超额 +5.0%——跌市里跑赢市场）。**滚动选枪**（每年只看上年成绩在两强间换枪，决策只用过去）全区间年化 +13.2%/Sharpe 0.68 > 池内最优单因子 0.65 → 选枪动作有正价值，但幅度小（+0.03）且 2022 换枪当年踩空（选了后来输的）——**同族二换一收益有限，扩池（异族因子）才是轮换的正路**。

19. **精选度战役判决（2026-09-08，预注册：用户定档散户持仓 3~20 只 → k∈{5,10,20} 一次扫完、仅按 IS Sharpe 选优、及格线 IS≥0.46，口径全锁死 band(0.10,0.50]/rebal=10/含费）**：IS Sharpe——k=5 0.62 / **k=10 0.79** / k=20 0.66 → **当选 k=10**；对当选档补跑 WFO 逐年严考：正 Sharpe **6/7**、最佳单年（2020 +34.7%）贡献 27% → **双考 PASS，产品二形态定档 = Top-10、每 10 交易日调仓**（全区间费后 +13.8%/0.66、回撤 -34.2%）。**诚实账**：①集中度代价=2026 年至今 -9.4%/-0.33 为该档最差年（k=100 同段 -2.0%），且总费用 70.7%（占初始资金）为广持仓的数倍；②k=5 全区间年化更高（+14.5%）但 IS 不占优，按预注册不选（防"看完成绩挑答案"）；③k=3 已探索消费（+18.0%/0.74、费吃毛利 78.7%）不参与判决；④**流程违规自曝**：--json 网格输出带出了 OOS 列（结论9⑤同类泄漏）——选优仅用 IS、未因 OOS 改参，且旧 SPLIT OOS 早已消费作废，实质无污染，但今后网格必须物理屏蔽 OOS 列再打印。

20. **仓位择时战役判决：FAIL（2026-09-08，`research/timing.py`，预注册 N∈{20,60,120}×off∈{0,0.5} 仅按 IS 选优）**：IS 当选 **MA20 + 降到半仓**（IS Sharpe 0.87 > 基线 0.79，短均线完胜长均线：MA60/120 的 IS 只有 0.33/-0.15，震荡市里慢信号一律被甩耳光）。全区间验收 **年化 +13.2%（低于基线 +13.8%）→ 预注册"三项同改善"第一票否决，判死**。**但防守端是压倒性胜利**，必须如实记下：最大回撤 **-34.2% → -22.1%**、Sharpe **0.66 → 0.79**、Calmar **0.40 → 0.60**、目标病灶 2026 段 **-9.4% → -4.2%**、2022 熊市 -1.1% → +3.2%（WFO 仍 6/7、最佳单年贡献 22%）。**方法论缺陷自曝（判决有效，但标尺定错了）**：给"防守层"定的及格线要求"年化也必须更高"，等于要求降仓者在牛市里赚得更多——2020~2026 是等权小票牛市，任何减仓必然少赚，这条线**在结构上不可能被满足**（同结论10①"Sharpe>0.5 定得比基准还低"同类错误：及格线必须衡量被测对象真正承诺的东西，防守层承诺的是回撤/风险调整收益，不是绝对收益）。**纪律处置**：本轮 MA20 数字已经看过=已消费，**不得事后改线改成 PASS**。**用户终裁（2026-09-08）：彻底判死、不再回头——择时路线关闭，Top-K 产品永远满仓，不设降仓开关**（接受 -34% 级回撤换取牛市满仓收益，防守改由"信号做深"承担；timing.py 保留作历史工具，勿再投入）。

21. **L4 挖掘回路实弹（2026-09-08，`research/dig.py`：propose→compile→judge→memorize 全自动，本地 Qwen3.8-Flash）**：2 轮试猎=**1 过 1 拒，零人工干预**。过线首猎 `a_retail_chase_quiet`（`rank(-mf_net_pct_l)*rank(-vlm_turnover)`，散户小单追逐×低换手）：**IS/neu ICIR 0.907=建库最高**、组合终裁 IS 年化 +8.3%/Sharpe 0.52 过基线 0.46 → 自动入册 candidate（升 product 仍须人工 WFO 预注册，机器不自封产品）。七形态对策全部落码：#1 InfraError 故障/业务分流、#2 表达式+秩相关双去重、#3 对话只喂 IS 数字、#4 一次假设一次判决+机制拉黑、#6 轮数预算+连续零产出停机、#7 dig_log.jsonl 心跳线。**三个工程教训（当日抓当日修）**：①**未来函数漏洞**——编译器白名单原含 `fwd_ret_5` 标签列，LLM 写它就是假 IC=1.0；`feature_columns()` 统一剔除 fwd_* 并实测拒绝（既有因子回归无恙）；②**去重门向量化 bug 误杀全部候选**（`groupby.corr()` 切片取到对角恒 1.0，结论14 同款教训第三犯）——改显式逐日 rank+corr；**被误杀的 a_low_vol_quiet（低波×低换手，IS ICIR 0.767）值得人工重提**（09-09 复盘修正：预判错，重提后被真去重门拒——与 vlm_turnover 秩相关 0.93，本就是低换手换皮，非独立信号）；③qwen3 reasoning 模型正文与思维链双通道，JSON 在 content、提取取第一个合法对象（探枪时 JSON mode/tool_calls 均原生可用，256K 上下文）。**当前态势**：回路可无人值守（硬预算+心跳），待用户批准批量夜跑。

22. **两新候选 WFO 双过 + 三族轮换判死（2026-09-09）**：L4 首两 candidate 过 WFO 严考——`a_retail_chase_quiet` 正 Sharpe **6/7**、最佳单年贡献 39%、全区间 +10.3%/0.56；`a_quality_quiet` **6/7**、40%、+10.0%/0.53（两者均达 product 硬门槛，待用户确认转正）。**轮换扩池首战即终战**：三族池（反转×低波/散户追逐×低换手/质量×缩量）滚动选枪全区间 **0.55 < 池内最优单因子 0.65 → FAIL**。病灶清晰：按"上年 Sharpe"选枪 6 年换错 3 年（2022/2024/2026 全踩空）——**单年成绩本身是噪声，追它=追噪声**。结论18 的预言"扩异族池是轮换正路"**被 09-09 复证否定**：问题不在池的同族异族，在轮换机制本身（改选枪窗口=调参救活，纪律禁止）。**产品形态定案维持一锤子：a_rev_x_lowvol Top-10 满仓**；轮换/选枪路线关闭。挖掘的增益走"多因子等权合成"另役评估（预注册后再打）。

23. **L4 首批批量挖掘战果与家族簇警示（2026-09-09）**：`dig run --rounds 20 --patience 5` 实跑 17 轮收工（预算+停机双闸生效），**过线 6 枪**（IS Sharpe 0.49~0.61，全部 candidate 入册）：`a_qiet_main_inflow`(0.61)/`a_main_vwap_quiet`(0.55)/`a_main_inflow_short_oversold`(0.53)/`a_main_auc_cool`(0.51)/`a_main_inflow_low_volume`(0.50)/`a_main_corr_quiet`(0.49)；拒收 8（IC 不及格/组合不及格/换皮去重）、畸变 3（LLM 输出畸形，重试即可）。**命中率 6/17≈35% 远高于彩排期——但这是危险信号不是好消息**：6 条表达式**全部含 mf_net_pct_main（主力净流入）**，LLM 在"资金流×安静度"母题上反复微调配方，去重门（秩相关>0.9）挡得住换皮挡不住"同原料不同配方"。**对策（合成战役前置纪律）**：新候选逐个过 WFO 后才入池；合成时同原料族视为一支枪计权重；回喂 prompt 需加"禁用已用过原料"多样性约束（下批挖前改）。数据面：账本 29 因子在册（含 dig 中间态）。战役序（用户 09-09 定）：P0 合成 → P1 分钟量比领先度 → P2 回填/signals → P3 新闻情绪（通道不通）。

24. **合成战役 FAIL：第二次被数据否决（2026-09-09）**：`synth.py`（族折减等权合成，产物走 alpha 契约）建成；池=四族【现役|散户追逐|财务质量|主力净流入(6 支均分)】，预注册线写死（IS Sharpe>0.632 且 WFO 正年≥6/7）。判决：**IS 0.611 < 0.632 → FAIL**（WFO 6/7 达标但按"任一不过即 FAIL"执行，不许挑好看的报）。合成枪全区间 +11.5%/0.63 vs 现役 +12.6%/0.65——**稀释没有换来分散收益**。病灶与轮换同根：池内成员虽名义异族，实际全是"安静小票"同一脾气体质，截面相关性高，等权合成只会把最强那支稀释掉。**两次独立战役（轮换、合成）证明同一件事：在池子同质化解决之前，任何组合花样都不增值——增益只能来自真正不同的信号源**（P1 分钟量比领先度正是冲这个去的）。纪律自查：全程未事后改线/换池；`synth.py` 保留为工具，`a_synth_f4` 产物留档不入库不荐股。

25. **P1 分钟战役开工即中靶：量能特征是真异族（2026-09-09，初检待复核）**：管道 `fetch min_agg`（云端把 1m 聚合成日频三窗 3m/30m/90m，只取 09:30-11:00，51s/交易日）+ `build_min`（7 特征）全通，产物直接走 alpha 契约进 eval。**2020-06 单月初检**：m_vdecay30（先放量为抢跑）IS ICIR **-0.663**、m_vconc3（前3分钟量占比）**-0.604**、m_pull30（冲高回落）+0.441 过线；纯价格特征（开盘涨幅/延续）全弱——**用户"量比优先于价格"的判断应验**。与现役全部族原料秩相关 ≤0.22（vs a_rev_x_lowvol -0.19）——**轮换/合成两役缺的"真异族"首次出现**。方向为负=开盘抢跑放量者随后跑输（散户追开盘热的钱在送钱）。m_lead30（行业 PIT 内率先启动，用户风向标设想）单月 -0.208 暂不及格，待其余月份。**判决纪律**：抽月数据不能跑净值回测，试点判决=4 个 IS 月 ICIR 跨月一致且 |ICIR|>0.25 + 分层价差方向一致；过线才立项全期回填（~22h）走组合终裁+WFO；2026-06 为 OOS 只看一次。5 抽月取数 09-09 中午进行中（断点续跑安全）。

26. **P1 试点终判：分钟开盘特征在 5 日与 1 日两周期全军覆没（2026-09-09 收盘前）**：5 抽月取数完成（93 片 42.6 万行，网络故障一次 fail-fast 续跑零污染，通道与断点机制再验证）。**5 日标签判决（事前线：IS 4 月每月|ICIR|>0.25 且同号）**：唯一过线 m_pull30（冲高回落 +0.32/+0.50/+0.32/+0.29），但 OOS 2026-06 一眼 **ICIR -0.060 反号归零 → FAIL**（该 OOS 已消费）；m_vdecay30/m_vconc3 在 2023-06 双双变脸（-0.7→-0.0 / -0.8→+0.5）= 2023 微结构真翻转，非标签噪声。**1 日标签公正复检（新试验新预注册，因用户假设本属日内周期）**：仍全灭——m_pull30 四月同号(+0.46/+0.42/+0.52/+0.19)差 0.06 压线未过，其余同号性更差。**结论：①"行业率先启动=风向标"（m_lead30）在 5 日/1 日两周期均证伪；②"开盘抢跑放量随后跑输"仅在 2020-2022 稳定（减法信号候选），2023 起翻转不可产品化；③价格类开盘特征（gap/trend）从头到尾无信号**。留下的资产：min_agg 日频原料底座（5 抽月）+ 全链路管道（fetch 云端聚合/build/eval 接入），做日内策略或全期回填时随时可启用；**教训（入纪律）：验证假设前先核对标签周期与假设周期是否匹配——用 5 日标签审日内直觉是本次施工顺序的失误，用户质疑后才补 1 日复检**。

27. **signals/ 荐股层建成即抓出 alpha 产物滞后缺口（2026-09-09）**：`signals.py` 输出当日应持 Top-10 与买卖动作，三条纪律撑起正确性——①选股直调 `backtest.topk_weights`（同一实现，杜绝两套逻辑漂移，教训 2）；②持仓=数据纯函数（全量权重矩阵取最后两个调仓行做差额），无状态文件可记歪；③独立代码复算 08-25 选股与输出逐票一致后才算数。**首跑即暴露真 bug**：a_rev_x_lowvol 荐了旧一天的单——alpha 产物是编译时点快照，update 重建宽表后不会自动跟新（实查产物停 09-07 而宽表到 09-08），若不查，荐股会永远滞后且内部毫无报错。修复：signals 内置 `_refresh_alpha`（产物截止<宽表截止→按账本表达式自动重编译，产物属派生层允许覆盖）。执行口径如实声明：回测按信号日收盘价成交、实盘次日下单存在约 1 日漂移，不做纸面修饰，实盘以实测漂移为准。**通用教训：任何"编译/派生产物 + 增量更新数据"的组合都存在快照滞后风险，消费端必须核对产物截止与数据截止**。

28. **候选通胀危机与三步歼灭战（2026-09-10）**：dig 监工 7×24 一夜 23 批后 candidate 从 14 爆到 44（全未过 WFO）、心跳 malformed 率约五成——**IS 及格线是为单发人工审设计的，机器轰炸下失效（44 支按运气也该有十几支"IS 过线"）**。三步处置：①补 09-09 数据（聚宽 Cookie 会话被回收是老毛病，`.env` 换票即愈，fail-fast 设计让认证故障没有静默成业务结果）；②`wfo_screen.py` 让 44 候选全过产品级 WFO 严考（口径=dig 组合终裁同配置 k=100/rebal=10/band(0.10,0.50]，方向与 IS RankIC 符号联动；先用 v_amt 对照校验脚本=判它 PASS 与人工转正史吻合）——**43 PASS / 1 FAIL（bb_yest 最佳单年贡献 133% 靠一年吃饭，打回 retired）**；③dig 及格线预注册 v2 收紧（经用户批准）：IC 初裁 |IS ICIR| 0.25→**0.40**、组合终裁 IS Sharpe 0.46→**0.55**（现役产品同档），dig.py 与 PROPOSE.md 三处同步。**关键认知：逐年大考筛不掉同族——43/44 过线恰恰证明病灶不是假货而是家族泛滥（各自都灵、相互无新信息，与轮换/合成两判死同根）**。新门实战首日即拦下旧标准能混过的枪（IS 0.43 被拒、秩相关 0.93 换皮被顶回），产出率 20 支/夜 → ~2 支/批。**遗留待拍板**：43 支过线候选的家族取舍（血缘矩阵已出，见结论29）。**09-10 用户批准执行（家族代表制）**：按 WFO 成绩降序点将、与已入选者血缘>0.85 者退役——34 过线代表保留 / 9 家族重复退役（每支档案注明代表，可复活）/ 3 product 不动；退役名单 a_qiet_main_inflow、a_auction_vol_cold、a_main_inflow_low_volume、a_auc_smooth_limit_cool、a_pe_goodwill_no_limit、vlm_turnover、a_auc_limit_cool、a_main_vwap_quiet、a_cold_limit_vwap。同日补考新挖 2 支（a_retail_calm_vol 0.59 / a_retail_calm_corr 0.62）均 PASS 入编。**09-10 又修 eval 真 bug**：dig 批 10:44 rc=1 崩溃非 infra——`eval._quantiles` 对极端稀疏日（2020-04-08 仅 1 只票有有效因子值）做 qcut 五分位，分位边界重复直接抛异常崩整批；改 `duplicates="drop"` 并层，微型复现测试（单日 1 值）验证通过。教训：**评估器对截面有效数下界必须鲁棒**，新因子覆盖天然稀疏（上市晚/字段缺失），此类日必然出现。**下一步主战场=合成战役三战**（池=35 代表+3 产品，血缘矩阵是族折减权重的直接输入；前两判死教训：预注册线必须衡量真承诺）。

29. **血缘矩阵 famcorr 与小截面伪相关坑（2026-09-10）**：`famcorr.py` 量全体 candidate+product（48 支）两两日截面秩相关（抽日 541 天、跨日取最大绝对值，与 dig 换皮闸同语义）。**首跑产出大片 1.00 假血缘——独立抽查（近 60 日逐日秩相关）揭穿：v_amt vs a_cash_quality_low_pv_corr 矩阵报 1.00、实测均值仅 +0.05**。根因=新因子上市初期覆盖仅几十只票，小截面日秩相关随机冲 ±1，"取跨日最大值"恰好焊死这些极值。修复=dig.max_corr 同款保护（全因子齐值完整截面且 ≥300 只才计入），重跑后有效截面 515/541 天、结果可信。**教训（与结论14/21 同族第四犯）：任何截面统计必须先保证两侧同集合且截面够肥；"最大值"聚合对极值污染零抵抗，出新数字先抽查两对已知不相关的枪做哨兵**。家族结构（修正版）：真正贴脸的只有 主力净流入族内 0.90 一对、a_quiet_two↔a_auction_vol_cold 0.89、a_retail_calm_vol↔现役 a_retail_chase_quiet 0.89 等零星几对；**其余 40+ 支彼此血缘 ≤0.88，"同族泛滥"的定量程度比直觉轻**——43 支里大量"财务×安静"配方彼此 0.6-0.8，属"近亲"而非"双胞胎"，取舍需按家族代表制而非简单阈值一刀切。

30. **ML 组合侦察演习：LightGBM 逐年向前 vs 头名单枪（2026-09-10，用户批准"先试效果"，定性=演习非转正通道）**：`ml_combo.py`——46 在册因子日截面 rank 当特征、fwd_ret_5 截面 rank 当标签（纪律8 同周期），逐年向前防泄漏（测 Y 年只见 (Y-1) 年 12 月中前数据+12 天隔离带、只预测本年——中途抓出并修掉"后年模型覆写前年预测"的隐性泄漏）。**信号层大获全胜**：合成信号逐日 RankIC +0.112 / ICIR 5.27 / 正率 80%（单枪普遍 0.03 级，强 3-4 倍）。**组合层平局收场**：同窗 2022 起对决（双方都截 ML 有预测的起点），ml_lgbm +14.1%/Sharpe 0.64/回撤 -37.3% vs a_small_value +14.4%/0.63/-41.0%；同窗 5 年 Sharpe 均值 0.76 vs 头名 0.78。**解读：横截面排序能力翻几倍，却没换成组合超额——因为 Top-100 带内选股早被"小而静/value 倾斜"占满，ML 赢在篮子内部排序、输在篮子选择，与结论24（浓咖啡兑浓咖啡）同根：同质弹药下 ML 榨不出新钱**。附带观察：ML 2025 +43%/Shp1.89（贡献 47% 贴线过 WFO 最佳单年线）＝微盘贝塔择时，不可依赖。**三战设计输入**：ML 值得再战，但前提换弹药——100 支池+正交维度（回填/新家族）出现后，ML vs 族折减等权 vs 头名单枪三方同场预注册竞技；特征重要度打印应补进下版。产物 derived/alpha/ml_lgbm.parquet 不入账本（演习纪律）。教训：**评估 ML 组合器必须信号层（RankIC）与组合层（Top-K 含费）双口径——只看 IC 会误判大胜，只看 Sharpe 会误判无用**。

31. **ML 组合侦察二番战：142 支大池 vs 结论30 的 46 支——弹药三倍，结果纹丝不动（2026-09-14，用户令"用组合测试 137 因子收益率"）**：ml_combo.py 原班口径重跑（特征 46→142、逐年向前、含费 k=100 同窗）。信号层 RankIC +0.1139/ICIR 5.32/正率 80%（与 46 支版 0.112/5.27 几乎相同）；**同窗 2022~2026.09 组合层 +14.1%/Sharpe 0.64，与 46 支版的 +14.1%/0.64 一字不差**，仍与 a_small_value 同窗 0.63 打平；正 Sharpe 4/5 年、2025 贡献 49% 贴 WFO 线（微盘贝塔择时复发）。**定论：ML 组合器的产出对"同族弹药数量"完全免疫——3 倍枪数换来 0 增益，瓶颈 100% 在正交维度不在数量/算法**。三战若开打，ML 的意义只能建立在回填/新家族等正交原料上；单纯把 dig 从 142 挖到 1000 不会改变 ML 战果（用户"挖 1000"路线照跑，但预期按此校准）。耗时教训：142 特征逐年重训全程 ~90 分钟（训练集逐年膨胀至 648 万行），GPU 不可用（vllm 占满 4×L20 + pip 版 LGBM 无 CUDA，详见 HANDOFF 算力实情）。**【09-14 更正】"同质弹药/篮子早已占满"的归因被诊断推翻**：band 内 Top-100 篮子实测与 a_small_value 平均交集仅 4.3 只（Jaccard 0.022）——ML 选的根本不是同一个篮子；打平的真因见结论35（band 掐断 ML 头段增益）。"同族数量免疫"（142 支 vs 46 支一字不差）不受影响、仍然成立。

32. **聚宽同款引擎本地对表：我方 eval 获得外部互验（2026-09-14）**：jqfactor_analyzer 1.1.0（聚宽"因子评估"页开源后端）装入项目内 `.tools/jqfa-venv` 隔离老栈（pandas 2.2/numpy 1.26；严禁污染 jaycode 主环境——pip 版 LGBM/jqdatasdk/seaborn 等依赖链逐个补齐），因子与行情全程本地（规则 8 零外泄）。**IC 互验近乎完美：a_rev_x_lowvol 聚宽引擎 IC +0.0819 vs 我方 ric_raw +0.0825（差 0.7%）**——宽表/标签/编译管线与十年老牌引擎口径一致，我方尺子无系统性偏差。ICIR 数字差系单位制（聚宽年化 ×√(252/5) vs 我方期频直除；换算后 4.0 vs 4.9 同量级，我方略高因行业中性口径更纯）。jqfa 分层多空年化（+21~27%）不可与我方多头含费篮子直接比（全市场五分层纸面对冲、不含费、不能融券）。踩坑：jqfa 在 pandas≥2.2 下三处兼容性绝症（groupby 外键重复拼索引/weights 列缺失/因子分位列名冲突），quantize_factor 打算法级补丁、分层收益改直算兜底（jqfa_check.py 内注明）。副产品：`.tools/jqfa-venv`+`research/jqfa_check.py` 留存，日后要聚宽独门指标（IC 衰减/因子自相关/行业归因）随时可出。

33. **行业线第一战役：日频 PIT 行业归属全史落地 + 5 列聚合原料入宽表（2026-09-14）**：季末快照表缺陷查实（as-of 最多滞后 92 天、2020Q1 空窗 83 天），而行业要进因子原料必须精确到日；聚宽无行业变更历史接口 → 只能逐日 `get_industry` 快照暴刷。`fetch industry_daily` 半月一片（11 交易日）+ 断点续跑，152 片全史回填 **793.4 万行 / 1666 交易日**。**读回验收三项全绿**：①逐日覆盖 1666/1666 与交易日历零缺失零多余；②与旧季末表 27 个重叠季末日 12.95 万对键 sw_l1 一致率 **99.9985%**（仅 2 处长停牌票双方同为 NaN）；③NaN 分布正常（sw_l1/sw_l2/zjw≈0，jq_l1 缺 0.26%=部分票无聚宽分类属正常），每日截面 3708~5218 只、32 个申万一级。**探针片良性冗余**：探针与正式片前 10 天重叠 37175 行，逐字段一致率 1.000000，build 端按主键去重消化（industry.parquet 7,897,291 = 7,934,466 − 37,175 对平）。**L2 落地**：宽表新增 5 列行业聚合原料 `ind_r_20d`（行业20日动量）/ `ind_r_1` / `ind_amt_5_20`（行业量能热度5/20）/ `ind_rs_20d`（个股相对行业强度差）/ `ind_lead_20`（行业内动量分位），industry.parquet 换源为日频 PIT；原料数值五项读回复算全过（ind_rs 恒等式 0 违例、行业动量手工复算精确匹配、行业内分位均值 0.5010 且榜首=1.0）；check 体检门全绿、eval 无回归（a_rev_x_lowvol RankIC 仍 +0.0825）。**dig 零改动即吃新矿**：`alpha.feature_columns()` 直读宽表 schema，新列自动进 DSL 白名单。**冒烟非判决**：`rank(-ind_rs_20d)`（行业内弱者反转）full RankIC +0.0459/ICIR 0.392、OOS/neu +0.0553——量级约头名一半，方向呼应结论2"行业相对化后信号更纯净"，但此为链路冒烟、未经 WFO 与血缘审查，不作因子结论。**PIT 真实性对抗审计（09-14 补查，这是"取对没"的决定性证据）**：若聚宽拿最新分类回填历史，六年变更事件应趋近 0——实测 **1102 次 sw_l1 变更**，且与真实史实严丝合缝：①2021-12-13 单日 648 票 = 申万 2021 版分类大改版；②采掘→煤炭、化工→基础化工更名全落在换版日；③个案 000815 于 2024-07-30 轻工→计算机（借壳重组时点）；④编码↔名称 32↔32 完美双射；⑤jq_l1 缺失 0.26% 集中在 107 只票（退市整理期等，其中 99.8% 行 sw_l1 仍有值，行业线不受影响）。**待验**：ML 在行业原料加入后 RankIC 是否上台阶（结论31 已证同族数量免疫，行业属正交维度，这是本轮真正的判决点）。

34. **解禁数据战役 + 一次"未审先判"的纠错（2026-09-14，用户以《实践论》点破）**：探矿时我只试了 `get_dividend/get_income/STK_*` 等**顶层函数名**就下结论"聚宽研究环境不提供分红/股东/事件数据"——用户回"没有调研就没有发言权，实践论"。复查文档后改用 `finance.run_query(query(finance.STK_xxx))` 路线，**实测当场出数：分红送转 STK_XR_XD、十大股东、股东人数 STK_HOLDER_NUM、AH 溢价**（`dir(finance)` 只见 5 个属性属懒加载，不能据此判死）。教训入方法论第 0 条注脚：**接口"不存在"的结论只能由"穷举所有调用姿势后的实测"得出，函数名不在命名空间 ≠ 数据不可得**。**解禁战役落地**：主力接口 `get_locked_shares(stock_list, start_date, end_date, forward_count)`（真实签名实测取得，`date=/count=/expect_date=` 全部 TypeError）；字段语义**不靠猜、用行情对账实证**——取样本票当日 close 与市值反推股本，逐票核算 `rate1≡解禁股数/总股本`、`rate2≡解禁股数/解禁前流通股本`（六票全中，误差 1e-4 级）。`fetch unlock` 半年片 + 断点续跑，全史回填 **14 片 / 17,529 行 / 4310 只票 / 1670 解禁日**，成本极低（全市场单月批量查 ~1s，比行业暴刷便宜两个量级）。**读回验收**：区间 2019-11-01~2026-09-11 零 NaN、`day,code` 零重复；年度事件 2020~2024 各 2200~3600 起、量级合理；**发现 49 行解禁日落休市日**（全是法定节假日，如 2020-01-31 春节、2020-05-03 五一、2021-02-12 春节），语义应为"顺延至下一交易日流通"→ fetch 端补 `next_trading_day` 列（首版误用 `bisect_right` 把 17529 行全部顺延、暴露边界错，改 `bisect_left` 后顺延行精确回落到 45 行 = 49 减去 2026-09-11 之后无交易日者）。**L2 落地 3 列事件原料**：`unl_today`（当日解禁量/总股本，宽表 >0 占比 0.22%）、`unl_sum20`（过去 20 日累计）、`unl_next20`（**(t, t+20] 未来窗**——解禁日程提前数月公告故当日已知，属合法前视，与"未来收益"型未来函数有本质区别）；数值验收 = 抽 300 票逐日校验两个滚窗 **0 错值**、个案 000004 @2020-12-31 next20=0.1891 与手算完全一致。**ML 判决的诚实修正**：上一轮汇报的"ML 行业复跑"（ml_combo_ind.log，+11.2%/0.60、RankIC +0.1143，与结论31 的 0.114 一字不差）**其实根本没测到行业原料**——`ml_combo` 特征取自账本在册因子，而 `ind_*` 要等 dig 挖成因子入库才会间接进入，数字不变正是"没吃到"的证据（差点把它当成"行业无增量"的判决，实为无效试验）。已给 ml_combo 补 `--extra`（宽表原料列直接当特征，红线自查：原料进 ML 特征 ≠ 帮因子过闸，不违反行业中性化纪律）与 `--tag`（对照组独立产物名不覆盖基线），真正判决实验 = 145 账本因子 + 8 列原料直用，后台跑中（tag=ml_lgbm_raw）。**✅ 09-15 验毕**：原料直喂反而稀释（RankIC 0.099/同窗 Sharpe 0.45 < 基线 0.114/0.72）——**行业/解禁的正确用法=dig 编译成因子、过质检门入池后间接进 ML，直喂宽表列这条路判死**（详见结论35 判决）。

35. **ML"无增益"真因诊断：band 规则掐断了 ML 的头段增益（2026-09-14，用户"组合没增益太难以接受"逼出的三项诊断）**：不服气之下把"信号层赢 3~4 倍、组合层打平"的反差拆开查，三刀全中同一要害。**诊断1 篮子重叠**：band 内 Top-100，ml_lgbm 与 a_small_value 平均交集 4.3 只 / Jaccard 0.022——**结论30/31"篮子早已占满/同质弹药"的归因当场推翻**（ML 选的完全是另一个篮子）。**诊断2 增益位置**：ML 打分分组的前向 5 日年化（2022 起）= 头 0-10% 段 **+27.1%** > band 买入带(10-50%] +21.2% ≫ 后 50% −1.0%，且头段是微盘聚集区（市值中位 32 亿、45%<30亿，对照 band 带 52.6 亿）；分年 5 年里 4 年头段跑赢带 5~11pp，**唯 2024（微盘踩踏）头段反跑输 3.2pp**——band 既是增益的闸门也是那次踩踏的刹车。**诊断3 单变量消融**：同一 ml_lgbm 信号只改 band——现行(0.10,0.50] +16.3%/0.72 → 全开(0,1] **+20.2%/0.83**（+0.11）；a_small_value 同测 0.63→0.60 不升反微降（其极端端确实差，band 对它是保护），**"band 与信号方向假设联动的脾性"首次量化**。**口径注（防日后误读）**：诊断用 09-14 15:48 重建的 ml_lgbm（账本 144 candidate）且**窗截 2022 起**，故 0.72 高于结论31 报的 0.64（账本 137 支时）与 ml_combo 自印的 0.60（全区间含 2020-21 两个无预测的空仓年，被摊薄）；诊断的有效性在"同一信号只改 band"的内部对照，绝对水位随账本漂移属正常。**定性**：ML 的横截面排序本事有一半长在打分头端，而结论19 为单枪定的"跳过最极端 10%"恰好整段切除它——**增益不是没有，是被产品级规则合规地截掉了**；band 当初对 a_rev_x_lowvol 校准，从未针对组合器信号重审（规则也有适用域）。**处置（用户拍板"预注册严验再定"）**：预注册已写死 HANDOFF 第三节#13——候选臂 A1(0,1]/A2(0.05,1]、及格线 P1 WFO≥5/7 且最佳年<50%、P2 同窗增量 Sharpe≥+0.15（高于观察值 +0.11 防均值回归）、P3 压力年 2022/2024 与回撤闸、P4 费用≤1.5x，任一不过即 FAIL 且本诊断消费作废；PASS 也只把参数带入合成三战，不直接改产品。**✅ 09-15 判决 FAIL（按事前线执行）**：主审 ml_lgbm_raw 同窗仅 0.45（原料直喂反而稀释——行业/解禁的正确通道是 dig 过闸入池）；A1 全开 ml_lgbm 增量 +0.11 不过 P2(+0.15)、2024 年差对照 11.7pp 不过 P3、回撤踩线。**band 维持原样、头端敞口路线关闭、诊断消费作废**；ml_lgbm 现行口径 = 同窗 +16.3%/0.72。**教训（入方法论第4条外延）：裁判规则本身也是待验假设**——为 A 信号校准的风控参数套到 B 信号上可能恰好吃掉 B 的 alpha，"规则全绿≠规则正确"，反常结果（信号层与组合层背离）值得花半天做机制解剖而不是接受表面结论。**战役外侦察补充（同日，用户问"k=3/4 呢"）**：ml_lgbm 按持仓数 × band 二维扫描（同窗 2022 起含费）——k=3：band 现行 +0.5%/0.17 vs 全开 +17.1%/0.67；k=4：+5.6%/0.33 vs +20.7%/0.80；k=10（产品形态）：+17.8%/0.74 vs +20.4%/0.82；k=100：+16.3%/0.72 vs +20.2%/0.83。**发现① band 的伤害与集中度成乘数级**（k=3 时被迫在 10-50% 段挑 3 只，ML 头段 alpha 整排没收，Sharpe 崩至 0.17）——band 只在大 k"摊薄买入带"时才是温和刹车；**发现② 集中换不来 Sharpe 只换来颠簸**（全开后各 k 的 Sharpe 0.67~0.83 打平，回撤 −40%→−48%、费用至 40%，k=3/4 实为单票赌局）；k=10 是全开体制的甜蜜点下沿。**"band 全开+k=10"数字诱人但未过预注册（战役只审 k=100），要立产品新形态须另役事前定线**。

36. **事件减法首战 + 日内下午段开工（含一根前复权真雷）+ 择时复议（2026-09-15，用户三连令）**：①**减法≠做空**（用户"没有融券怎么做减法"点出的概念纠偏）：负面名单减法=只把钱从坏票旁挪开，零融券需求。**解禁减法首战**（ml_lgbm k=100 同窗 2022 起，避 `unl_next20`≥阈值的票）：V0 无减法 +16.3%/0.72 → V2 避≥5%股本 **+17.0%/0.74/回撤收窄 0.5pp**（5 年 4 年改善）——**方向真实但偏薄**，机理：Top-100 买入带内解禁票本就稀少（小市值偏好天然避开解禁大票）。定性=方向对、弹药少，事件减法弹药库应向**分红登记/质押/业绩预告/减持公告**扩（finance.run_query 路线已通）。②**日内下午段（HANDOFF#15）开工**：`fetch min_agg_pm`（13:00~15:00 一m → 日频午后/尾盘30m/尾盘15m/收盘竞价 18 列）建成；**探针抓到前复权真雷**：`get_price` 不显式传 `fq` 默认 **pre（前复权）**——c_day 与日线 close 偏差中位 7%、一致率仅 16%，根因是前复权因子按"当前"回算=自带未来函数（PROJECT C 类早有判词、模板却踩了）；补 `fq=None` 后 c_day/c_call 与日线 close**一致率 100%**，下午量占全日 42.9% 符合 A 股 U 型量曲线。P1 旧模板（开盘段）同带此因子但既有特征全是比值（因子在分子分母同现自动抵消）→ 不回炉、留案。**教训（rules 第5节外延）：取数模板里 get_price 必须显式 fq=None，"默认值"是数据正确性的隐形雷**。5 抽月+2026-08 回填进行中（断点续跑）。③**择时复议（用户："仓位减法可以考虑的"，推翻 09-08 用户终裁）**：尊重改判（终裁权本就在用户）；重开前提=**换对尺子**——09-08 役的及格线"年化必须同升"在牛市结构上必杀防守层（当时已自曝标尺错误），新役线应以**防守指标为主**（Calmar/回撤/压力年改善）+ 年化不设同升门槛，数据与 timing.py 工具全在，预注册见 HANDOFF 第三节#16。

37. **Alpha101 收编战役 + 事件弹药扩编 + run_query 5000 行截断暗雷（2026-09-15，用户"兼收并蓄"拍板）**：①**同榜对表**（自家 160 + 官方 82 支同尺：5 日采样 fwd_ret_5）：自家霸 IS 榜前四（头名 0.81 vs 官方头名 alpha_016 0.69），但 **OOS 榜差距抹平**（官方头名 alpha_016 OOS 0.79 全场第一，精英存活率官方 11% > 我方 7%）——IS 霸榜有"考试机制"水分（自家枪都是 IS 筛过的，官方枪没参加过我方质检）。**教训：对未经同一考试筛选的对手比 IS 成绩=胜之不武，跨源比较必须看 OOS。**②**官方公式权威来源问题**：官方文档页 JS 渲染抓不到、jqlib 不在 PyPI、arxiv 抓取失配——最终用**自家云端通道 `inspect.getsource(jqlib.alpha101)`** 一打印全取回（101 支公式+get_pool+复权处理，100KB 存档 `data/raw/jq/alpha_ref/official_src.txt`，比论文更权威含 A 股适配）。③**A 腿（本地复算）主动判死转 B 腿（官方原值直取）**：Top15 公式涉及 16 种算子（decay_linear/ts_rank/product/**截面** correlation），复刻 jqlib 语义（截面 vs 时序相关、NaN 传染、股票池）风险高，且对答案本身也要同等机时 → B 腿同成本拿官方原值零语义风险。`fetch_alpha` 升级 `--step/--cols/--tag`（`get_all_alpha_values` 第二参数实测只认 '101'/'191'，不能按列筛 → 云端裁列再下载省 90% 体积）；Top15 日频全史 163 片回填中，4 天探针验收过（15 列齐、NaN 最高 alpha_094 15.8%）。收编用法：Top15 直入 ML 特征池（不经我方质检门、由 ML 重要性+逐年验证裁决）；官方头部结构骨架喂 dig prompt（喂骨架不喂答案）。④**三件套终审（30 个表名穷举 getattr，结论34 纪律的执行）**：**增减持 STK_SHAREHOLDERS_SHARE_CHANGE ✅、质押 STK_SHARES_PLEDGE ✅（翻案：探针1 用 STK_PLEDGE 判死是拼写错， dir() 懒加载不可枚举故必须穷举候选名）**；业绩预告/快报/回购共 14 个拼写变体全 NO_ATTR → **判死成立（研究环境确无）**。减法弹药改道=解禁+减持+ST 三件套。白送战利品：股东人数/十大股东（TOP10+FLOATING_TOP10）/指数成分权重全部实测出数。⑤**run_query 5000 行截断暗雷（教训5/读回验证再次救场）**：增减持首版全史 74,397 行看似正常，读回发现 **14/15 片恰好 5000 行整**=run_query 单次上限截断；修复=按 id 排序 + offset 分页（PAGE=4000 直到短页），重取得 **172,934 行=截断版 2.3 倍**。全库审计：其余 finance 用法均为 `end_date==单期 × code.in_(≤1500)` 双切片、单片远低于 5000，历史数据不受此雷污染。**教训（rules 第5节外延）：分片落盘出现"整数阈值行"（5000/整万）=截断铁证，行数分布必须做读回检查项**。⑥**两表验收**：增减持 172,934 行/4893 票/type{1=减持 124,690, 0=增持 48,244}/重复键全为合法多阶段（end_date 全不同）/change_ratio≥10% 仅 0.011%（极端值 build 端截断处理）；质押 97,512 行/2913 票，**pledge_number 缺 48.6% 查实=解除质押行股数在 unpledged_number**（48.57% 行该列有值，语义互补非脏数据）。两表挂 update 日更（today 档，公告多在盘后）。**减法弹药扩编判决在即**：解禁(已 +0.1pp)→ 增减持/质押进负面名单与宽表原料后重跑减法战役。

38. **收编判决役开局 + 数据缺口全景审计（2026-09-15 晚，用户"还差什么数据"）**：①**Top15 收编件收官**：163 片全史日频回填完成，`ml_combo --consolidate` 合片验收满分（772 万行 × 15 列、分片边界重叠行值一致校验通过、非空率 83~100% 符合各算子特性）→ 落 `derived/alpha/alpha_XXX.parquet` 与编译产物同构。②**判决役设计（同池单变量，吸收结论35 教训）**：`ml_combo --battle`=mlb_base（在册池）vs mlb_off（+官方 15 列）同种子逐年向前，各跑 现行band(0.10,0.50]/全开 两格成四格——主判据=现行 band 下同窗差（对照 +16.3%/0.72、RankIC 0.1149），全开格只做**归因**（若增益只在全开显形=band 重审的又一条证据，不产生改线效力）；附送官方列 importance 名次解剖。**看门狗教训（rules#5 再犯一次当场认）**：首版 watcher 完成条件写松，回填 158/163 时抢跑开打——半截数据版当场停用作废重挂（条件=回填进程真实退出+合片验收通过才开打）。③**10 接口一次云端审计（结论34 纪律，一次 exec 全审）**：**Alpha191 原值 ✅（188/191 列非全 NaN，与 101 同接口同深度）=弹药库直接扩一倍**，`fetch_alpha --lib 191` 已扩参、榜对榜（step=5，与 alpha_board 同尺）163 片回填在跑，收官后并入榜单对表选 Top 批打二判决；**STK_XR_XD 除权除息 ✅47 列**（分红登记率/股息率域翻案可建，此前"分红判死"系未审）；分析师一致预期（consensus 2 变体）/宏观 shibor+macro_base_item/概念成分/mtss_stocks 等 7 项 ImportError 判死。**真缺口定档=分析师预期域（与量价族真低血缘，机构域经典 alpha，聚宽无 → 候选 AKShare 只读通道）+ 宏观利率（只服务择时复议役，役前再补）**；零成本回锅肉=mtss 两融/股东人数/十大股东已落盘未进宽表。④**行业推荐快检（用户问"行业推荐行不行"）**：先答现状（持仓散布 8 行业、无集中）再验证——行业 20 日动量轮动 Top1/3/5 全亏（-7.5%~-0.3%，Sharpe≤0.16）、Bottom1 冷门策略 +11.2%/0.52 仍不及选股层 0.66——**A股行业层动量是反的，追热=负期望；行业信息正确位置=原料（已进宽表 ind_* 被 dig 吃）而非指挥层**。

39. **收编判决 FAIL + 宇宙缩窄证伪 + 涨停生态双枪判死（2026-09-16，"换思路"三连役）**：①**收编判决役 FAIL（预注册线，看数前写死）**：mlb_base（167 支在册）vs mlb_off（+官方Top15）同池双臂×双band 四格——现行 band 同窗 +14.0%/0.64 vs +14.6%/0.65（差 +0.6pp/+0.01，不过线）；RankIC 0.1152 vs 0.1137 **平台未破**；全开格 base 反优 3.5pp。**最有价值的解剖**：官方列 importance 高居 #4/#5/#7/#8（ML 吃了弹药），但信号层不动——**交易面同款信息榨到 RankIC≈0.115 就是铁顶，与弹药来源（自家/官方101）无关**；191 二判决优先级降级（同款弹药预期无增益）。band 疑点攒第二条证据（base 全开 0.88 vs 现行 0.64、2024 分年两格打平 0.40/0.45——上役 FAIL 主因消失），**改线仍须预注册新役**。②**宇宙缩窄侦察（用户"限定30日内涨停才是目标"直觉）**：产品枪同口径四宇宙——全A +13.7%/0.66、近20日曾涨停(16%保留) +3.1%/0.25、近20日未涨停 +8.0%/0.45、近60日未涨停 +13.4%/0.66——**任何"宏观认知式"砍宇宙都持平或更差，产品枪在自家全域最强**；行业层追热同日证伪（见38④）。③**涨停生态双枪判死（用户拍板"换思路主攻涨停"后的第一战，竞价真实可成交口径、剔一字买不进、费 0.0031、标签 T+1/T+2 与兑现周期一致——rules#8）**：枪1 首板接力 59,854 票日：T+1 费后 **−0.45%、胜率43%、七年年年负**；一字对照 +0.86% 但排不到（逆向选择）；枪2 切片网（断板反包/断板深度/封板×竞价涨幅/双确认 全网格）：**没有一个切片费后正期望**，且呈"越确认越死"梯度（竞价高开7%+ → T+1 −2.20%）——**涨停生态对费后系统策略判死，赚钱的票不给上车的门**；散户打板叙事与我方数据不兼容，此路焊死不再反复。④**战役内两次自纠（当场认，rules#1/#5）**：a) 枪2 注释把预想数字"−0.75%"当"枪1实锤"预写进文档——未审先判第三次犯，出真实数字后当场重写注释；b) 枪1 首版样本 370 票日（应 6.4 万），`groupby.apply` 返回索引错位致 first_bord 大面积失效——**侦察脚本必须目测样本量级对账**（370 vs 64,334=170 倍差，看行数才抓到）；附 daily 分片 concat 混 dtype 致 object 除法崩，to_numeric coerce 修。⑤**路线改判（主要矛盾重排）**：交易面加弹药（191/预期域优先级）与涨停生态双双降级；现役引擎三役升主战场——**band 重审役（数字上最大肉块：全开 0.88 vs 现行 0.64）→ 减法二役（解禁∪减持∪质押）→ 择时复议役**；转债战场（双低轮动，历史 Sharpe 1.5~2 档）作为唯一未探的高地挂起候补，等三役出数或用户点单。

40. **band 重审役 II 判决 FAIL（2026-09-16，预注册 #19，改线级重测）**：同一 mlb_base 打分（167 支池）双臂组合层判决——A0 现行 band(0.10,0.50] 同窗2022+ +14.0%/0.64、dd −38.6%；A1 全开 同窗 +21.9%/0.88、dd −42.0%。**P1 增量 +0.244 ≥ +0.15 ✅；P2 最差年 2024 −2.2pp ≥ −10pp ✅（上役死因 11.7pp 这次消失）；P3 回撤恶化 3.4pp > 3pp ❌——差 0.4pp 撞线，总判决 FAIL，band 永久维持、路线第三次永不再提**。如实三笔：①**敏感性披露**：若按上役原线（dd 5pp）今日应判 PASS——本役线定 3pp 是收紧（用户批准在手），判后不许回头改线，此披露只存档不作翻案依据；②**预注册瑕疵自认**：方案里写"照抄上役三关"，实际 P2（5pp→10pp，放宽）/P3（5pp→3pp，收紧）均有改写——意图是"不放水"但字面不符，教训=**重测役的线必须逐字复制原件，任何"优化"都是事后裁量的入口**；③全开臂质量确实高（2022 转正 +5.7%、2026 +18.7% vs +1.3%），代价 dd 破 40%——**"收益换回撤"的交换率（+7.9pp 同窗年化换 3.4pp 回撤）已被精确量出来，供未来任何杠杆/仓位役作为已知参数引用，但 band 本身维持不变**。执行器 `data/derived/band_retrial2.py`（判决只按事前线自动判，无二次裁量）。

41. **减法二役判止：负面名单扩容在 mlb_base 上无增益（2026-09-16，五臂同口径侦察）**：增减持/质押首进宽表（build 新列 hc_reduce20/hc_in20/pg_ratio/pg_new20）后，mlb_base k=100/band 现行/含费五臂——V0 无减法 +13.1%/0.61；V1 避解禁≥5% +12.6%/0.59；V2 +减持≥2% +12.5%/0.59；V3 +质押≥50% +12.6%/0.59；V4 质押30激进 +12.5%/0.59。**全部减法臂 ≤ 对照**（差异在噪声区内偏负）。机理定案：Top-100 买入带内坏事件票本就稀少（宇宙保留仍 97.7%+，小市值偏好天然避开减持/高质押大票）+ ML 组合器已把事件信息间接定价——**首战 ml_lgbm 上 +0.7pp 的薄增益是弱信号时代的残留，组合器越强、负面名单越冗余**。事件减法线（解禁/减持/质押）就此收兵，弹药库不再扩建（业绩预告/回购已判死、分红 XR_XD 转因子原料候选）。**build 端三雷修复（读回验证当场抓，教训5/读回纪律的又一次实证）**：①公告日落非交易日→精确 merge 整行丢事件（19.1% 公告在周末/停盘日！质押覆盖只剩 47%）→ 公告日映射"≥公告日首个交易日"生效（`_to_tday`，与解禁 next_trading_day 同口径）；②change_ratio 465% 级脏行 → clip(0,20)；③pg_new20 除零 inf → 分母 where(>0)。**口径披露**：本脚本 V0(0.61) 与 band 役 A0(0.64) 差 3pp=脚本自建 Portfolio 与 wfo._curve 实现微差——臂间相对比较有效，绝对值以 _curve 为准（教训2 的又一课：跨脚本对数字先对口径）。执行器 `data/derived/subtraction2.py`。

42. **中证500 宇宙判死：我们的 alpha 活在中盘以下（2026-09-16，用户问"只买中证500会如何"）**：成分季末快照 PIT 已取回（28 快照×500 票，`raw/jq/index_members/csi500.csv`，云端一次 exec）。四臂同口径——产品枪 全A +13.2%/0.64 vs **只买500 +2.7%/0.23（2022 年 −28%）**；ML 组合 全A +9.0%/0.51 vs **只买500 +1.9%/0.20**。与宇宙缩窄（结论39②）同构定案：**alpha 本体=小微盘的安静反转溢价，中证500 恰好切掉主产区**（2025 年 500 内 +20% 尚可=微盘行情外溢，2022 微盘崩盘中盘补跌则 −28% 露底）。"限定大盘蓝筹更稳"的直觉第二次被数据否决；宇宙纪律定案：**全A 窄进出不设、只做可交易性剔除**。执行器 `data/derived/csi500_test.py`（as-of 快照有 ≤1 个月调样滞后，侦察精度，已披露）。

43. **下午段全史回填开工 + jqcli 认证自愈机制（2026-09-16 傍晚，用户"先挂全史回填等结果"）**：①**侦察见矿苗（`data/derived/pm_probe.py`，124 天×58 万票日）**：收盘竞价量占比 `v_call_r` RankIC −0.047/ICIR −0.96/胜率 19%、量能后置度 `v_pm_share` +0.024/+0.47——**两者与现役安静族截面相关仅 0.14/0.16=收编判死以来首个"真异族"信号**（竞价放量→未来 5 日跌，机构出货窗机理，fetch 假设兑现一半）；但分年翻脸（v_call_r 2020/22 负、2023/26 正，超噪声范围）→ 124 天不足以终审，**全史回填=终审前提**。②**回填战**：1503 片×65s≈27h，`data/derived/backfill_pm.sh` 三段设计=按月分片断点续跑+每晚 20:00-21:30 让位日更（通道锁 fail-fast 故主动错峰）+**认证自愈**（实测：长跑云端必回收 Cookie→`jqcli auth login` 用 .env 的 JQCLI_USERNAME/PASSWORD 可自动重登，无需人工贴 Cookie——此前一直以为 Cookie 过期须人工干预，本次 16:20 撞坑当场翻案）。③收官动作=全样本重验六特征分年 IC，同号且 |IC|>0.02 才谈进宽表/回锅肉战役。

44. **收编二役（打分层混分）FAIL：收编路线两机制全判完、永久关闭（2026-09-16，用户"把聚宽比较好的因子纳入进来"）**：与首役（结论39=官方列进 ML 特征池）机制不同——本役官方 15 支**绕过 ML**，IS 窗(2020-21)定向后合成"官方复合枪"，与 mlb_base 打分层 50/50 rank 平均，走完全相同产品流程（k=100/band/含费/同脚本双臂）。预注册三关照抄 band 重审役逐字（结论40 教训）：base 同窗 +12.8%/0.60 dd−38.6% vs mix +12.9%/0.61 dd−40.7%——**P1 增量 +0.007 ≪ +0.15 FAIL**（P2 最差年 2024 −7.9pp PASS、P3 回撤恶化 2.1pp PASS）。**定案：官方枪的信息已被 ML 组合器完全吸收（首役 importance 前排但不动 IC）+ 打分层混入只会稀释主信号（年度解剖：2026 +17.8pp 领先 vs 2024 −8pp 拖累，一年神一年坑）**。收编=ML 池、打分层两机制双双判死，路线永久关闭、第四次不许再提；官方资产保留价值=骨架喂 dig（结论37 通道，已在产）+ IS/OOS 榜对表标尺。执行器 `data/derived/absorb2.py`。

45. **无人值守双链并跑纪律（2026-09-17 晨间调研当场抓获并修复）**：用户"调研搞清楚现状"→ 实地盘点抓到昨晚事故：**回填 v1 的让位窗只在段间查时钟，而段=整年 243 片连续跑不看钟 → 20:05 日更链撞通道锁 fail-fast 整链作废（09-16 日线缺一天），且失败无重试**。修复三件：①回填 v2=月段（~23 分钟/段）+19:45-21:45 让位窗段内也看钟；②夜间链 v2=update 循环等锁（30 分钟×8 轮）而非一次即弃；③09-16 缺口今晚自动补。**教训 generalize：fail-fast 锁适合单进程互斥，不适合定时任务链——调度链遇锁必须"等"，双链并跑要显式设计握手（谁让位、让多久、怎么重试），否则深夜互踩无人知晓。** 现状盘点（08:45 实测）：回填 859/1627 片（2020 全+2021 大部）；dig 在册 206（candidate 203+product 3，一夜 +36）；日更至 09-15；账本全推、工作树干净。

46. **行业基金思路侦察：因子分数聚合到行业层判否，个股层不可绕过（2026-09-17，用户思路"因子排序选强行业→买行业ETF（听说还T+0）"）**：先纠偏两个事实——场内股票型 ETF 一律 **T+1**（T+0 只有跨境/债/金/商品 ETF，与 5~10 日调仓节奏本无冲突）；与结论38④"行业动量追热判死"不冲突——那是动量信号，本侦察是**个股因子分数按申万一级成员均值聚合**当行业信号，此路未测过。侦察（mlb_xgb 打分 2022~今，每 10 日换仓，ETF 佣金 0.1%/边）：**信号层**：行业截面 RankIC +0.049、正率 56%、5 年 4 年同号（2025 −0.009 翻脸）——信息确实传导到行业层，但个股层 0.1166 被聚合压缩掉一半以上（~30 个截面观测，成员均值把头部信号全稀释）。**组合层判死**：Top-1/2/3/5 行业组合 = +2.2%/0.21、+4.8%/0.33、+4.8%/0.33、+4.5%/0.32，**全线跑输全行业等权基准（+13.8%/0.53）**，更远逊个股层 mlb_xgb 同窗 +15.9%/0.71——**行业聚合是损失函数：alpha 长在个股截面的头 10% 里，等权平均进 30 个行业桶后被摊平；连"买对了强势行业"都没换来钱**（选行业还不如闭眼等权）。ETF 低费率（0.1% vs 0.13%）与容量优势补不回信号损失。判否不立项，行业信息正确位置维持结论38④ 定案=原料进宽表被 dig/ML 吃，不做指挥层。**侦察中再抓基准陷阱（教训 2/读回纪律实证）**：首版用 mlb_xgb join 样本算行业日收益，基准得 −21.2% 的反常值——诊断实锤：mlb 产物经可交易过滤**不含当日涨停/异动票**（日均剔 235 只、其 r_1 均值 +3.2%、26% 是涨停档），join 样本系统性压低基准 14bp/日；修复=信号用 join 样本、**收益与标签一律回全样本 features.parquet**。通用外延：**任何用"过滤后的打分产物"算组合收益/基准的脚本，都要先问被过滤掉的票去哪了——涨停恰是收益最高的截面，静默剔除=基准腰斩**。执行器 `data/derived/probe_ind_fund.py`。

47. **散户形态矩阵（k×调仓频率）侦察：加频是净损失，"少持仓+高换手"直觉双杀；唯一诱人格在已判死的全开臂（2026-09-17，用户"我们不可能买100只股，我们是散户"，点名 k=3×每2/3/4日）**：mlb_xgb 同窗 2022+ 含费，band 两臂×k∈{3,5,10}×rebal∈{2,3,4,10} 共 24 格（wfo._curve 同实现，与判决役口径一致）。**发现① 调仓频率越低越好，在本矩阵内单调成立**：rebal=2 全线最差（现行 band 全负 −8.8%~−10.6%，全开也仅 +3~5%）——2 日换仓的费用+噪声把信号磨光；每个 k 的最优格都落在 rebal=10。**发现② 现行 band 下减持仓单调变差**：k=3/5/10/100 @rebal10 = +5.5%/0.33 → +7.8%/0.41 → +10.9%/0.52 → +15.9%/0.71，集中度代价肉眼可见。**发现③ 用户点名的 k=3×每2日=全矩阵最差之一**（现行 −8.8%/−0.16 dd−63%）。**发现④ 唯一亮点与纪律红线**：全开 band 的 k=3/k=5×rebal=10 = +27.1%/0.95、+25.7%/0.94（年化反超 k=100 基线），呼应结论35"band 伤害与集中度成乘数级"——但 dd −49%~−53% 更狠，且**全开臂已在结论40 正式判决 FAIL（P3 回撤线）**，此数字只能作为"散户形态正式役"的候选臂情报，不许直接上岗。**产品口径澄清（用户此前误以为回测=100只/10日）**：判决 yardstick 用 k=100 是标尺不是仓位；真实荐股产品=Top-10 每 10 交易日（结论19 预注册定档），ML 信号标签=fwd_ret_5（预测 5 日、组合 10 日一换）。**纪律声明：本矩阵 24 格数字全部计为已消费**，若开"散户形态转正役"（候选臂 k∈{3,5,10}、band 两臂、rebal=10 为主，频率轴已死不再纳入），及格线必须另立且不得向本矩阵成绩看齐（结论40 教训：重测线逐字复制/事前写死）。执行器 `data/derived/probe_freq_k.py`（首版 dd 用多列 DataFrame 算出 −1348% 假值，读回抓到、改 ret 单列重跑）。

48. **通达信 TPD 指标收编测试：单票择时无选择性，截面方向与战法反号——技术买卖点叙事第三次被数据掉头（2026-09-17，用户给出 TPD 原始公式"TPD=EMA(C,7)-EMA((O+C)/2,7)，MATPD=MA(TPD,6)，金叉买死叉卖"）**：按原公式忠实编译（后复权 open/close——EMA 跨日递推必须复权价，真实价除息跳空污染指标；EMA α=2/(N+1) 与通达信等价），产出三变体 alpha 产物（t_tpd_gap/t_tpd_state/t_tpd_ngap）进 eval 同门 + 事件研究双检。**判决① 单票择时（原战法语义）无选择性**：金叉事件 32.7 万次/6.7 年（≈每天全市场 7% 的票同时"买入信号"，年 4.7 万次可成交——毫无稀缺性），其后 5 日 +0.37% vs 无条件基准 +0.28%，抬升 0.09pp 费后归零；**死叉"卖出"后 5 日反而 +0.38%（卖了踏空）**；2022 金叉后收益为负。**判决② 截面化后信号为真但弱且与战法反号**：金叉状态（TPD>MATPD=短线冲高形态）RankIC −0.030、neu ICIR −0.305、OOS/neu −0.311 不衰减——**方向是"金叉→跑输"**，与结论0/1"放量/追热负向"同一个 A 股反转微结构：技术战法的买点恰是截面意义上的卖点。量级仅为在库头名的 1/3、不过 dig 新线（0.40），且血缘 t_tpd_ngap vs v_close_loc 秩相关 0.51、vs v_vwap_dev 0.48——**一半血缘是自家"收盘强度"老料，无独立入池价值**。**处置**：不宽表化不入池，产物留 derived/alpha/ 备查；与用户思路结合的正确答案=此类技术形态的正确归宿是**当 ML 特征原料被消化**（ML 会自动学到它的反号与交互），而不是当独立买卖指挥——已在结论25/26（开盘抢跑）与结论39（涨停生态）两次验证同一规律：**A股散户技术叙事在我们数据上普遍反号或无选择性，这是世界观级旁证，不是偶例**。执行器 `data/derived/probe_tpd.py`。

49. **TPD 补丁"水下金叉"复核：用户战法补丁版首次测出真结构——剂量效应+穿越时刻增量+低血缘三条齐全，但 2026 全档翻负是活病灶（2026-09-17，用户追问"不仅看金叉还要看柱子，下方的柱子"）**：按经典"水下金叉"（TPD<0 深水区才认金叉）把结论48 事件按柱子深度切开重测，**结果与裸金叉（判死）完全不同——这次数据站在用户这边**：①**剂量效应单调**：水下金叉 +0.36% > 水上金叉 +0.22%，且水下内按柱子深度三档=最深 **+0.76%** / 中间 +0.14% / 最浅 +0.17%（越深越灵，噪声很难编出单调性）；②**归因探针（probe_tpd3）三臂拆解**：限定"深水下状态"（TPD/收盘<当日截面水下 1/3 分位）内——A 金叉日 +0.99%/5日（胜率 52.8%，7 年 6 正）vs B 状态全部日 +0.60% vs C 金叉后 1~2 日 +0.42%——**钱的大头在"深跌状态"（老反转 β），但金叉穿越时刻相对状态延续仍有 +0.4~0.6pp 增量**（A>C 且 A>B，穿越时点本身有价值，不是纯换皮）；③**血缘干净**：A 信号与 r_20d 截面秩相关仅 −0.074——不是 20 日反转换皮（区别于结论48 裸 TPD 的 0.5 血缘）。**但三盆冷水必须如实**：(a) **2026 年（今年）全档翻负**（A 臂 −1.24%、A∩ML 高分组 −0.57%）——当下体制正与该信号作对，任何转正役 WFO 逐年考它都会被抓；(b) 0/1 事件信号在全截面 RankIC≈0（−0.0016，事件占比仅 1.4%，eval 尺子对稀疏事件天然失明——**IC 判不了事件因子，终裁必须走组合**，呼应结论14"IC 榜与钱榜分道"）；(c) 与 ML 叠加（A∩mlb_xgb 高分 30%）= +0.34%/5日、日均 11 只、2022~2025 四年为正但 2026 −0.57%。**定性：这是本轮用户技术直觉第三次被数据部分证实（前两次=量能负向结论1、水下柱子的方向感），也是第一个"事件形态"级真苗**——但它同时带着"今年正在亏钱"的活病灶，与下午段矿（结论43 分年翻脸）同类。**处置**：全部数字计为已消费；若要转正=预注册"事件因子役"（A 信号驱动 Top-10 事件组合+WFO 逐年严考，2026 臂如实参考；预注册线看数前写死、按结论40 教训逐字定线）；ML 喂料路线暂缓（事件列可作特征但 2026 翻脸期喂入风险同前）。执行器 `data/derived/probe_tpd2/3/4.py`（三雷当场修：features.date 是 str 勿转 datetime、pan×features 全表 merge 撞名 close、探针 2/3 连环踩同款——**教训：merge 只带键+所需列**）。

50. **新批成色盘点 + 276 支大池 XGB 复训：第六次撞同一铁顶，弹药数量免疫从 LGB 复现到 XGB（2026-09-20，用户"看看新因子成色，树模型组合训练测试年化 Sharpe"）**：①**深度思考 prompt 新批（09-17 起 78 支）成色=与旧批持平略优**：IS Sharpe 中位 0.68（旧批 0.68 持平）、IS ICIR 中位 **0.67 vs 旧批 0.61（+10%）**、RankIC 中位 0.043 持平；**新矿确实被吃到**——78 支原料分布：行业 ind_ 35、解禁 unl_ 28、两融 mt_ 28、质押 pg_ 20、增减持 hc_ 20（这些域此前几乎无人问津），头名 `a_hardcash_indr1stable_supplylight`(IS Shp 0.99)/`a_pledge_unwind_newlow_calm_vol`(0.91，质押解除机制)/`a_reduce_unlock_certainty_lowvol`(0.89，减持确定性)——**机制新颖度肉眼可见提升（质押解除/解禁×散户追逐/减持确定性都是老配方里没有的故事），四步法方向正确，但 IS 数字层面尚不构成跳变**。②**XGB 276 支大池复训**（同超参同种子同标签逐年向前，与 206 支池逐字同口径）：信号层 RankIC **+0.1163**/ICIR 5.53/正率 79%（206 支池 0.1166，纹丝不动）；组合层同窗 2022+ **+13.5%/0.63** dd −35.2%（206 支池 +15.9%/0.71，微降落在噪声区）；全区间 +9.3%/0.53；分年 2022 −1.4%/0.06、2023 +14.2%/0.97、2024 +13.3%/0.52、2025 +44.0%/1.99、2026 −1.8%/0.04。**铁顶第六次确认：46→142→206→276 四代池子的 RankIC 全部锁在 0.114~0.117，"弹药数量免疫"从 LGB 复现到 XGB**（结论31 的同族免疫规律换个裁判照样成立）。③**纪律待办（制度线已越线，优先于任何新战役）**：在册 276 支中有约 78 支**积压未过 wfo_screen 大考**——"每 +20 支跑一轮 wfo_screen+famcorr+家族代表制"的制度线（结论28 定）早已越过，池子噪声上升是 276 池组合微降的合理嫌疑；**下轮动作=先跑制度清洗（机器活，全自动），XGB 转正基线仍以清洗后池的 mlb_xgb（+15.9%/0.71）为准**，mlb_xgb276 只作侦察产物不入账本。④**如实的另一面**：ML 组合至今在**组合层仍未超越 wfo_screen 头名单枪** a_small_value（0.78/年化 +18.2%），信号层强 3~4 倍换不成组合超额的老病灶（结论30/35）未解——**当前产品最优形态候选仍是"头名单枪 k=10 band 现行"或已挂起的散户形态役**，ML 的价值定位=打分器与弹药利用率放大器，不是自动提收益。**踩坑记录**：账本 `backtest` 字段存的是 **JSON 字符串**而非 dict（factorlib add 未反序列化），分析脚本必须先 json.loads——首版直取键崩 TypeError。执行器 `data/derived/probe_newbatch.py` / `xgb276_report.py`（数字计为已消费）。

51. **行业闸门实验（"先选行业再选股"两次选择）：增益不稳健，判"不是新信息源"——行业层只能当原料不能当闸门（2026-09-20，用户"做两次选择如何，类似于行业因子"）**：三臂同引擎（结论46 简化净值口径，费 0.13%、band=(0.10,0.50]、每10日）——A0 全市场 ML Top-k / A1 行业聚合 ML 分强行业 Top-N 闸门内选 / A2 弱行业闸门对照，k∈{10,5}×N∈{5,10} 共 8 组。**结果=方向不对称但整体不稳健**：① k=10 时正闸门 Top-5 = 同窗 +20.4%/0.89 胜过基线 +18.4%/0.78（且 A2 弱行业 Top-5 显著差 +12.4%/0.55——**存在方向不对称=有真信号成分**）；② 但 **k=5 时正闸门反而输基线**（+17.8%/0.78 vs +19.1%/0.77），且 Top-10 闸门两 k 都输基线；③ **逐年大考不过**：正闸门 k=10 分年 2022 +20.9% vs 基线 −7.8%（大赢）↔ 2023 −2.9% vs 基线 +14.2%（大输）——**赢的年份与基线互相翻脸，WFO 逐年正 Sharpe 率反而不如基线稳**。定性：行业聚合 ML 分≈行业动量+ML 行业贝塔的混合体（结论38④ 行业动量判死、结论46 行业组合判死），闸门效应=在"行业贝塔顺风年"锦上添花、"逆风年"雪上加霜——**它不产生新信息，只放大行业贝塔的运气方差**。基线 8 组里全部 ≥+18.4% 且从不垫底，全市场选股依旧最优。**这与结论46 合并成完整定案：行业层当组合判死、当闸门判不稳健、当原料(特征列)是唯一正确位置（ind_* 列已在宽表、dig 在吃）**。用户"宏观认知→行业层"路线至此三种用法全部实测完毕。纪律：本实验 8+8 组数字计为已消费；除非出现**独立行业信息源**（分析师行业预期/行业资金流独有数据），行业层战役永不再开。执行器 `data/derived/probe_gate.py`（与 wfo._curve 官方引擎有口径差：A0 简化引擎 +18.4% vs 官方 +15.9%，差在涨停可成交近似与停牌处理——三臂互比公平，绝对数勿与官方战绩混用，如实标注）。

52. **"候选试错→确认进场→趋势跟踪"策略实测：用户直觉里"确认"是真金，“金字塔加仓”是累赘，追踪止损是回撤杀手——散户产品的最佳候选形态浮现（2026-09-20，用户设计"K日选N只试错，选对了才进，ATR跟踪加仓加注直到趋势消失"，并问"什么叫做选对了？"）**：六臂同引擎拆解各部件贡献（k=5 槽位、band=(0.10,0.50]、费 0.13%、收盘决策次日生效；"选对"标尺=σ14 单票日波动；确认=候选日后 H 日内收盘涨≥thr×σ14；金字塔=1/15 仓起步逐层加到 3/15；出场=收盘<入场后最高收盘−2.5×日 dollar 波动）。**A0 基线(直接买持10日) +8.2%/0.42 dd −45.6% 与官方引擎 k=5 +7.8%/0.41 吻合——本引擎可信**（顺带实锤：结论51 probe_gate 引擎每调仓日有 1 日空仓缝+费用双计，绝对数系统性虚高 ~7-10pp，臂间互比仍有效但绝对数勿与官方混用——已在该结论预先声明过口径差，本次拿到定量证据）。**六臂结果**：A0 +8.2%/0.42 dd−46%；F0 直接买+金字塔+追踪 +3.1%/0.26；F2 慢确认(5日1.5σ) −0.9%/−0.02；F4 直接买+追踪 +0.7%/0.14；F1 快确认(3日1σ)+金字塔+追踪 +6.5%/0.53 dd−11.9%；**F3 快确认+追踪·不加仓 = 同窗 +8.7%/0.61 dd −14.4%（六年最佳风险调整形态）**。F3 分年：2022 −3.7%、2023 +11.2%、2024 +14.7%、2025 +12.2%、**2026 +10.9%/0.83（基线们今年全趴窝它还在赚）**、在场率 73%、确认率 30%、平均持有 13.5 日。**三个干净结论**：①**"确认"是黄金部件**——同样带追踪，有确认 8.7%/0.61 vs 无确认 0.7%/0.14（F4），确认=3 天内涨≥1×日波动，70% 候选不确认自动不买=用户"试错"直觉的正确工程化；**且"选对"必须快而小**：慢确认(5日1.5σ)反而 −0.9%——涨多了才确认=追高，动量确认的边际在 3 日内衰减归零转负（与结论39 涨停接力"越确认越死"梯度同源）。②**金字塔加仓是累赘**：F3(不加) 8.7%/0.61 > F1(加仓) 6.5%/0.53——新高加码抬高成本基准，追踪出场时回吐更狠；用户设计的三部件里 ML 打分(N)、确认(选对)、跟踪(止损) 两个半有用，加仓归零。③**追踪止损是回撤杀手**：dd 从 −45.6% 砍到 −14.4%（2022 熊市 −27.2%→−3.7%），Calmar 0.60 **超过 k=100 官方基线的 0.42**——六年实测里首次有形态在 Calmar 上击败基线，且绝对收益(8.7%)与基线 k=5 相当、 Sharpe 差距(0.61 vs 0.71)主要来自 2025 单年（追踪止盈错过主升浪 +12% vs +71%）。**纪律声明**：参数(1σ/3日/2.5×)为一次性拍定未调优（少拧旋钮=少过拟合），但六臂数字已全部计为已消费；转正=预注册"确认跟踪役"（候选臂 F3 形态+官方 wfo 引擎逐年大考，及格线看数前逐字写死，禁止再调参后重测——如需调参只许在 2020-21 IS 窗内定死一次，OOS 只看一次）。**这一形态与"散户形态役"（结论47 情报）合并开打最经济：同一役考 k∈{3,5}×{band全开/现行}×{确认跟踪开关} 的主效应格，预注册一次判两案**。执行器 `data/derived/probe_pyramid.py`（首版 A0 忘关追踪污染成 7.5 日持有，读回与官方数对账当场抓获——**引擎正确性以"和官方引擎基线臂对得上账"为验收标准，第二次示范其价值**）。

53. **下午段全史终审：两颗候选矿双双判死，"分钟域半日聚合"路线收兵（2026-09-20，补 09-18 回填收官后的欠账）**：1627 片全史 773 万行对齐宽表 770 万行（侦察仅 124 天），尺与 pm_probe.py 逐字同款，判决线 09-16 写死=分年同号且 |逐年 IC|>0.02。**v_call_r**（竞价量/午后量比，侦察时 IC -0.047 的明星）全史 -0.0333/ICIR -0.51/胜率 28%——分年 2020 -0.140 与 2021 **+0.085 翻脸** → **FAIL**；**v_pm_share** 全史 +0.0159——2020 +0.104 一年独大、2021 起翻脸、逐年 |IC| 多在 0.02 线下 → **FAIL**。侦察期的"见矿"是 124 天小样本+2020 单年撑起的假象（**小样本见矿≠矿，分年大考才是矿**）。其余四特征 pm_ret/tail_slope/close_pull/tail_loc 量级全 ≤0.03 且血缘 0.38~0.67 一半老族换皮，无一过线。**定性：下午段半日形态在 fwd_ret_5 尺度无独立信息；分钟域要出矿得下沉到真·分钟级事件（连板分时/竞价逐笔），半日聚合弹药已打完**。raw 1627 片保留继续喂 dig 下午段原料域。执行器 `data/derived/pm_final_judge.py`（数字计为已消费）。

54. **确认跟踪灵敏度自审（用户问"为什么会错过趋势、算得对不对"）：机制如实+三道对账通过，并量化"追踪松紧=熊市保命↔牛市吃浪"交换机；同场抓获夜间链断更事故（已修）**：①错过 2025 主升浪=追踪止损体质而非 bug（急拉急回体制被回调打掉后须等新候选+确认才能回场）。扫描坐实单调性：**2025 随追踪放宽单调回升**（2.5xσ +12.2% → 8xσ **+39.0%**，平均持有 13.5→53.0 日），**2022 熊市同步单调变差**（-3.7%→-7.8%，dd -14.4%→-25.7%）——2.5x=熊市+23pp 保命、牛市-59pp 过路费；S2(8x) 同窗 +12.1%/0.57 年化更高但 dd -26%，两形态两种性格，**转正役须把追踪倍数当预注册变量公开考，禁止偷偷调优**。②**确认阈值 1σ 非幸运格**：0.5σ→+5.3%/0.38、**1σ→+8.7%/0.61**、2σ→-1.8%/-0.14 倒 V 且 2σ 崩（3 日涨 2σ=追高，与 F2 同源物理）。③**引擎对账**：A0 与官方 wfo k=5 吻合(+8.2%/0.42 vs +7.8%/0.41)+分年自洽+扫描单调，三道证据齐。**④ 夜间链断更事故（当日自查抓获）**：09-18/19 夜间链没跑——根因=09-17 v2"首考"是手动 bash 验证，**从未挂 crontab**（当时只有 dig 双链），"首考通过"被误记为"已排程"。**修复**：`5 20 * * 1-5 nightly.sh` 已挂常驻+当日补跑，欠 09-18/19 由 update 增量追平；**教训=交付自动化须调度器落位+验证一次真实自动触发，人工跑通≠自动化成立**（已入 rules#11）。

> 工程纪律与踩坑教训已固化到 `.trae/rules/rules.md` 第 5 节，本文件不再重复。

## 目录规划

`research/`（引擎工作区，入库）→ `src/`（正式区，入库）、`docs/`（文档，入库）、`data/`（gitignore）、`.tools/`（gitignore）。职责、流动与提升标准以 `.trae/rules/rules.md` R4 为唯一定义处。
