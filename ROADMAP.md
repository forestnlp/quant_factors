# quant_factors — 路线图

> **本文只管「走到哪、下一步做什么」**：进度、Todo、研究结论、风险。项目定位/架构/环境/数据见 [PROJECT.md](./PROJECT.md)，方法见 `.trae/rules/methodology.md`，约束见 `.trae/rules/project-rules.md`。
>
> 方法论回顾：闭环负反馈、五层过滤防过拟合、存量去重+增量创新、最小原型迭代。

## 一、进度

### ✅ Phase0 — 地基与清理
- [x] 项目重构：删除全部历史手工实验代码，四区→二区（`research/` → `src/`）
- [x] 本地数据底座：qlib 全市场日线 6135 只（含退市），2000→至今
- [x] 数据质量门 `data_fetcher.py audit`：bin 对齐 / 映射覆盖 / 新鲜度三项校验
- [x] 本地大模型接入（Qwen3.8-Flash，OpenAI 兼容）

### ✅ Phase1 — 单轮闭环打通（里程碑1 达成）
- [x] `llm_client.py` 调用封装 + `factor_miner.py` 生成→批量回测→落盘全链路
- [x] 批量评估：一次加载全因子库，双套指标（原始截面 + 行业中性）
- [x] 行业维度落盘：申万 31 行业指数转 qlib bin、成分股映射（覆盖 99.9%）
- [x] 首轮挖掘实证：`vwap_vol_reversal` 原始 RankICIR 0.104 → 中性后 **0.158**（真信号，非行业暴露）

### ✅ Phase1.5 — 行业实证与通道建设
- [x] 行业因子实证：行业自身信号弱（RankICIR 0.1~0.2）；**个股因子"行业相对化"后增强**（量能类 0.50→0.58）→ 结论：行业是"堆"的分层结构，不是独立信号层
- [x] 三轮交叉印证：手工扫描 / LLM 挖掘 / 行业实验均指向**量能类负向信号**（放量→未来收益偏低）
- [x] 聚宽云端通道打通（`.tools/jqcli`，Jupyter 协议远程执行）：实测可得**行业 PIT**、**财务公告日**、涨跌停价、市值/换手率/PB，数据新鲜度到当日
- [x] **聚宽 API 系统学习与权限实测**（文档本地化 `data/jq_docs/`，86 个函数；四轮探针逐项验证）：**集合竞价 `get_call_auction` 全市场可得（2010 至今，硬上限 5000 行/次）**、tick、预置因子库 260 个——推翻此前"竞价无免费源"判断
- [x] **取数机制定型**：云端执行 → `jq_out/*.csv` → `jqcli research download` → `data/jq_stage/`（实测 1.2MB 秒级；redis 降为备用通道）

### ✅ Phase1.6 — 数据治理与踩坑修复（2026-09-01）
- [x] 停止冒进：中止一夜 Loop 与竞价全量取数，回到"先把数据搞准搞对"的基线
- [x] 因子库除污：309 → 18 条（291 条 `Connection error.` 垃圾记录连同 metrics/空转轮次清除）
- [x] 修复**行业指数 bin 整体错位** Bug（15 个 2014 年后发布的行业 start 误写 0，数据平移到 2000~2012）→ 31 个指数重建，qlib 读数与源 CSV 逐日一致
- [x] 清除不完整竞价 bin（每标的仅 3 天）与 `jq_stage/`、`bt_export/` 残留；竞价 CSV（69 片 / 683 交易日 / 349 万行）体检无坏片，完整保留
- [x] `audit` 由 FAIL → **PASS**；LLM 服务与 qlib 基准（零 NaN）复测通过

### ⏳ Phase2 — 闭环迭代（当前）
- [x] **因子库沉淀 `factor_lib.py`**：SQLite 三表（factors/metrics/rounds），表达式规范化去重 + 非法率/同质化率/合格率统计
- [x] **取数工具化 `jq_pull`**：`data_fetcher.py` 生成脚本→云端跑→download→分片落盘（断点续跑）；竞价已取 2023-08~2026-05 共 683 交易日
- [ ] **竞价数据入 bin**：CSV → qlib bin 全量入库 + `audit` 验证（此前只入过 3 天验证样本，已清除，须重做）
- [ ] **修 `factor_miner` 失败分类**：基础设施故障（LLM 断连等）不得记为因子非法，须 fail-fast 中止而非静默入库
- [ ] **行业 PIT 落盘**：聚宽 `get_industry(security=全市场, date=)` 按季取数 → 折叠成 in/out date 区间表，替换当前快照（消除前视）
- [ ] **竞价因子库**：`get_call_auction` 按日分片取 2010 至今 → 构造竞价失衡/虚拟匹配价偏离等因子，接入评估链
- [ ] **基本面 PIT**：财务数据严格按公告日滞后对齐（报告期≠可得日，否则未来函数）
- [ ] **因子库沉淀**：SQLite/CSV 存历史因子全量（表达式、指标、失败原因），支持去重与相似度判定
- [ ] **失败复盘反馈链路**：把非法算子、失效逻辑回喂 Prompt（已有雏形，需系统化）
- [ ] **五层过滤**：训练集挖掘 → 验证集筛选 → 全样本 → 稳定性 → 相关性去重

### ⏳ Phase3 — 工程加固
- [ ] 常驻调度（限流、断点续跑、状态持久化）；执行沙箱隔离
- [ ] 去泡沫化回测：涨跌停买不进 + 停牌处理 + 真实滑点（涨跌停价已从聚宽可得）
- [ ] 监控告警（磁盘 / 任务 / 数据新鲜度）

### ⏳ Phase4 — 进化
- [ ] 因子变异衍生；Prompt 多样性引导（量价→基本面→波动率多维）
- [ ] 多因子合成"堆顶"组合；样本积累充分后评估 RL/BO 搜索

## 二、已确立的研究结论

1. **量能 > 价格**：放量类是最强量价方向，且为**负向**（放量→未来收益低），近 3 年震荡市成立。
2. **行业是分层不是信号层**：行业自身动量弱；但个股因子做行业相对化/中性化后信号更纯净。
3. **单因子 IC 绝对值普遍 <0.03**，须多因子合成；回测年化数字基于乐观假设，不可当实盘预期。
4. **本地 qlib 无基本面字段**，须外部补充——这是聚宽通道的核心价值。

## 三、踩坑教训（工程纪律，防复发）

1. **数据「落盘」≠「可用」——必须读回验证才算数。** 竞价 bin 写完自认为入库成功，实际每标的只有 3 天（早期验证样本），与 683 天的 CSV 完全脱节，直到抽样读回才暴露。**规约：任何写 bin 的操作，收尾必跑 `audit` + 抽样 `D.features` 读回比对源数据。**
2. **同一 Bug 不修两遍。** bin 的两个致命细节（头部 4 字节是 **float32** 起始索引、数据必须**补 NaN 到日历末尾**）在竞价写入处踩过并修好，行业写入 `industry_to_qlib` 却还是老的错写法，导致 15 个行业指数日期整体平移、读出来全是错的。**规约：写 bin 只有一个正确姿势，新写入路径必须对齐既有正确实现，且必须过 `audit` 这条通用门——audit 这次正是靠它抓出错位。**
3. **基础设施故障不能记成业务失败。** LLM 服务夜间断连，Loop 把 291 条 `Connection error.` 当"非法因子"写进因子库，非法率虚高到 94%；更糟的是这些噪音会通过失败回喂污染后续 Prompt，且 R5 之后 57 轮全是空转却无人察觉。**规约：连接/超时类异常必须 fail-fast 中止并告警，绝不入库；长跑任务须能被健康检查（轮次产出为 0 即异常）。**
4. **覆盖范围必须显式核对。** 一次只写 3 天数据、一次取数只到 2026-05（日历已到 2026-08），都是"跑完了"但范围与预期不符。**规约：取数/入库完成必须打印并核对覆盖区间（起止日期、标的数、行数、非 NaN 天数分布）。**
5. **并行后台任务要克制。** Loop 挖掘与竞价全量取数同时过夜跑，任一方故障都会污染另一方的判断，且无人值守时错误会整夜累积。**规约：跑基线期间一次只做一件需要验证的事；有验证产出的任务不在无人值守下扩到 60 批规模。**

## 四、风险登记

| 风险 | 等级 | 应对 |
|---|---|---|
| 聚宽通道风控/改版（jqcli 系逆向封装，研究执行功能仅上线 2 周） | 中 | 定位为**低频增强件**；已沉淀数据本地留存；源码 vendored 于 `.tools/` 可自行维护；协议变更则退回 AKShare |
| 财务数据用报告期对齐 → 未来函数 | **高** | 强制公告日滞后对齐，纳入质量门 |
| 行业归属用当前快照 → 前视 | 中 | Phase2 换 PIT 区间表；当前仅用于中性化（误差 <1%），禁止跑历史行业轮动 |
| LLM 高频产出伪因子 | 高 | 五层过滤 + 中性化双指标 + 失败样本回喂 |

> 本文件随进度更新，完成一项打勾。引擎模块职责见 PROJECT.md，目录规则见 project-rules.md，本文不再维护清单。
